---
phase: 01
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/alerts.py
  - backend/api/market.py
  - backend/api/strategies.py
  - backend/api/risk.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Database queries do not block the event loop"
    - "All API endpoints use AsyncSession"
  artifacts:
    - path: "backend/api/alerts.py"
      provides: "Async alert CRUD"
      contains: "AsyncSession"
    - path: "backend/api/market.py"
      provides: "Async market endpoints"
      contains: "AsyncSession"
    - path: "backend/api/strategies.py"
      provides: "Async strategy endpoints"
      contains: "AsyncSession"
    - path: "backend/api/risk.py"
      provides: "Async risk endpoints"
      contains: "get_db"
  key_links:
    - from: "backend/api/alerts.py"
      to: "backend/db/database.py"
      via: "get_async_db"
---

<objective>
Convert remaining synchronous API endpoints to use `AsyncSession` and `get_async_db`. This prevents database queries from blocking the main event loop, ensuring high concurrency and responsiveness.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/api/alerts.py
@backend/api/market.py
@backend/api/strategies.py
@backend/api/risk.py
@backend/db/database.py
</context>

<tasks>

<task type="auto">
  <name>Convert Alerts API to Async</name>
  <files>backend/api/alerts.py</files>
  <action>
    Update `create_alert`, `get_alert`, `get_alert_chat_context`, `update_alert`, `update_alert_status`, `bulk_update_status`:
    1. Change dependency `db: Session = Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`.
    2. Change `db.get(Model, id)` to `await db.get(Model, id)`.
    3. Change `db.add(obj); db.commit(); db.refresh(obj)` to `db.add(obj); await db.commit(); await db.refresh(obj)`.
    4. Change `db.query(...)` to `result = await db.execute(select(...)); result.scalars().all()`.
    5. Handle rollback with `await db.rollback()`.
    
    Note: `list_alerts` is already async.
  </action>
  <verify>grep "Depends(get_async_db)" backend/api/alerts.py</verify>
  <done>All endpoints in alerts.py use AsyncSession</done>
</task>

<task type="auto">
  <name>Convert Market API to Async</name>
  <files>backend/api/market.py</files>
  <action>
    Update `list_ai_decisions` and `market_stream`:
    1. Change dependency `db: Session = Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`.
    2. In `list_ai_decisions`, use `fetch_decisions_for_trade` (which is already async) or update the query to use `await db.execute(select(AIDecision)...)`.
    3. In `market_stream`, `get_current_session_ws` likely needs an async version or update `core/auth.py` if it performs DB checks.
    
    Note: Most market endpoints use `kraken_service` (async) and don't touch DB, but check carefully.
  </action>
  <verify>grep "Depends(get_async_db)" backend/api/market.py</verify>
  <done>All DB-touching endpoints in market.py use AsyncSession</done>
</task>

<task type="auto">
  <name>Convert Strategies API to Async</name>
  <files>backend/api/strategies.py</files>
  <action>
    Update all endpoints (`list_strategies`, `create_strategy`, etc.):
    1. Change dependency `db: Session = Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`.
    2. Convert all ORM operations to async style (`await db.execute(select(...))`).
    3. Use `selectinload` for relationships if accessing `rules` or `ai_modifications` requires join (though these are JSON columns, so likely immediate).
    4. Ensure `await db.commit()` and `await db.refresh()`.
  </action>
  <verify>grep "Depends(get_async_db)" backend/api/strategies.py</verify>
  <done>All endpoints in strategies.py use AsyncSession</done>
</task>

<task type="auto">
  <name>Convert Risk API to Async</name>
  <files>backend/api/risk.py</files>
  <action>
    Update `get_risk_settings`, `update_risk_settings`, `get_risk_score`:
    1. Change dependency `db: Session = Depends(get_db)` to `db: AsyncSession = Depends(get_async_db)`.
    2. Convert queries to `await db.execute(select(RiskSettings)...)`.
  </action>
  <verify>grep "Depends(get_async_db)" backend/api/risk.py</verify>
  <done>All endpoints in risk.py use AsyncSession</done>
</task>

</tasks>

<verification>
! grep "Depends(get_db)" backend/api/alerts.py
! grep "Depends(get_db)" backend/api/market.py
! grep "Depends(get_db)" backend/api/strategies.py
! grep "Depends(get_db)" backend/api/risk.py
</verification>

<success_criteria>
- No synchronous `Session` dependencies remain in the modified files
- All database operations are awaited
- Endpoints function correctly without blocking
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-07-SUMMARY.md`
</output>
