---
phase: 01-infrastructure-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/api/alerts.py
autonomous: true
must_haves:
  truths:
    - "Alert list results use cursor-based pagination so new insertions do not shift items between pages."
    - "Cursor responses include `next_cursor`/`prev_cursor` tokens plus page size metadata so the UI can fetch stable slices."
  artifacts:
    - path: "backend/api/alerts.py"
      provides: "Async alerts listing that returns cursor tokens, obeys filters, and documents the new pagination contract."
  key_links:
    - from: "backend/api/alerts.py"
      to: "backend/db/database.py#get_async_db"
      via: "Depends(get_async_db)"
      pattern: "Depends\(get_async_db\)"
---

<objective>
Deliver cursor-based pagination for the alert list so the UI can page through ordered alerts without duplicates.
Purpose: satisfy the infrastructure success criterion for paginated list endpoints and maintain the async DB strategy.
Output: `CursorPage`-style response, new cursor helpers, and `list_alerts` rewritten to use AsyncSession and cursor navigation tokens.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md
@.planning/phases/01-infrastructure-hardening/01-RESEARCH.md
@backend/api/alerts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cursor helpers for alerts</name>
  <files>backend/api/alerts.py</files>
  <action>Introduce a `CursorPage`/`CursorPageRequest` model inside `alerts.py` along with `encode_cursor(timestamp, id)` and `decode_cursor(cursor)` helpers (timestamp + ID, Base64). Update the response model to include `next_cursor`, `prev_cursor`, `limit`, and current `timestamp` so the frontend can stay in sync.</action>
  <verify>Run `python -m pytest backend/tests/test_market_api.py -k alerts` to ensure the new models import cleanly.</verify>
  <done>Cursor helper functions exist, `AlertListResponse` exposes tokens, and constants (default limit 25) are documented in the docstring.</done>
</task>

<task type="auto">
  <name>Task 2: Switch `list_alerts` to AsyncSession + cursor</name>
  <files>backend/api/alerts.py</files>
  <action>Update `list_alerts` to depend on `AsyncSession` via `get_async_db`, build a `select(Alert)` query that filters by the inbound parameters, and page using the cursor helpers instead of offsets (fetch `limit + 1` rows to decide `next_cursor`). Return the cursor response model with `alerts`, `next_cursor`, `prev_cursor`, and preserved filter metadata. Keep the severity/status/search filters and error handling intact.</action>
  <verify>Run `python -m pytest backend/tests/test_market_api.py -k alerts` to confirm the new async/cursor logic passes the existing alert tests.</verify>
  <done>`list_alerts` uses AsyncSession, obeys filters, encodes the next/prev cursor tokens, and never uses `.offset()`.</done>
</task>

</tasks>

<verification>
`python -m pytest backend/tests/test_market_api.py -k alerts` should pass and the response now includes cursor tokens while filtering still works.
</verification>

<success_criteria>
- Alert list returns `next_cursor`/`prev_cursor` tokens plus `limit` metadata, and the handler uses AsyncSession + cursor helpers instead of offset.
- The pytest alert tests confirm the refactor compiles and keeps filtering behavior (severity/status/search/since/until) intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-04-SUMMARY.md`
</output>
