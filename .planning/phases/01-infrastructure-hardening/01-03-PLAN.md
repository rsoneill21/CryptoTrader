---
phase: 01-infrastructure-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/db/models.py
  - backend/services/paper_trading_state.py
  - backend/core/paper_trading.py
autonomous: true
must_haves:
  truths:
    - "Paper trading sessions survive a backend restart because the full session snapshot is persisted and reloaded before agents resume."
    - "State persistence is backed by a dedicated DB table plus service so it can be archived, reset, and queried independently of the in-memory engine."
  artifacts:
    - path: "backend/db/models.py"
      provides: "paper_trading_states table + indexes that store session_id, JSON blob, active flag, and timestamps."
    - path: "backend/services/paper_trading_state.py"
      provides: "Async helpers to load, save, archive, and snapshot paper trading state without blocking."
    - path: "backend/core/paper_trading.py"
      provides: "PaperTradingEngine that writes state after each trade, reloads from DB on init, and exposes reset/archive APIs."
  key_links:
    - from: "backend/core/paper_trading.py"
      to: "backend/services/paper_trading_state.py"
      via: "PaperTradingStateService"
      pattern: "PaperTradingStateService"
    - from: "backend/services/paper_trading_state.py"
      to: "backend/db/models.py"
      via: "PaperTradingState"
      pattern: "paper_trading_states"
---

<objective>
Persist every paper trading session so its positions, cash, orders, and snapshots can be restored on restart.
Purpose: stop the paper trading state loss described in the roadmap success criteria and provide an archival trail for resets.
Output: `paper_trading_states` table + `PaperTradingStateService` and engine wiring that loads/saves snapshots asynchronously.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md
@.planning/phases/01-infrastructure-hardening/01-RESEARCH.md
@backend/db/models.py
@backend/core/paper_trading.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paper trading state table and service</name>
  <files>backend/db/models.py, backend/services/paper_trading_state.py</files>
  <action>Add a `PaperTradingState` model with `session_id`, `state_json`, `is_active`, `created_at`, `updated_at`, and `archived_at` columns (JSON serialized, indexed). Create `backend/services/paper_trading_state.py` that exposes `load_active_session(async_session)`, `save_state(async_session, session_id, state_payload)`, and `archive_session(async_session, session_id)` helpers and reuses the new model. The service should operate with `AsyncSession` and work with `AsyncEngine` hooks so persistence writes happen asynchronously without blocking the event loop.</action>
  <verify>1) Run `python backend/init_db.py` to ensure Alembic creates the `paper_trading_states` table. 2) Run `python -m pytest backend/tests/test_market_api.py -k paper` to confirm wiring does not break state-related test flows.</verify>
  <done>The new model exists with indexes, and the service can load, save, and archive states entirely via AsyncSession without blocking.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PaperTradingEngine to persistence</name>
  <files>backend/core/paper_trading.py</files>
  <action>Inject `PaperTradingStateService` into `PaperTradingEngine`, load the latest `PaperTradingSession` (paused state) on init, and persist snapshots after every signal handling + on periodic checkpoints. Provide methods for archiving the current session (`archive_current_session`) and resetting to a clean state. Use async persistence calls so the engine stays responsive during trade execution.</action>
  <verify>Re-run `python -m pytest backend/tests/test_market_api.py -k paper` to verify the new persistence hooks execute cleanly.</verify>
  <done>The engine loads a persisted snapshot before trades run, saves updates after processing decisions, and exposes archive/reset helpers that use the service.</done>
</task>

</tasks>

<verification>
`python backend/init_db.py` creates the `paper_trading_states` table, and `python -m pytest backend/tests/test_market_api.py -k paper` shows paper trading flows still pass.
</verification>

<success_criteria>
- Merchants have a `paper_trading_states` table plus service functions for load/save/archive.
- `PaperTradingEngine` loads persisted state on start and saves after every trade/snapshot, making state survive restarts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-03-SUMMARY.md`
</output>
