---
phase: 01-infrastructure-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/exceptions.py
  - backend/core/rate_limit.py
  - backend/api/errors.py
autonomous: true
must_haves:
  truths:
    - "The Redis-backed rate limiter fails closed (503 + Retry-After) whenever the circuit breaker detects Redis is unavailable."
    - "API errors bubble out as typed envelopes with `error.code`/`message`/`details` and propagate headers such as Retry-After when provided."
  artifacts:
    - path: "backend/core/exceptions.py"
      provides: "BaseAppException plus specialized RateLimitException/ServiceUnavailableException with header support."
    - path: "backend/core/rate_limit.py"
      provides: "Circuit-breaker-protected API specialist that raises the structured exceptions when Redis fails."
    - path: "backend/api/errors.py"
      provides: "Handler wiring that converts BaseAppException into RFC9457-style responses and forwards headers."
  key_links:
    - from: "backend/core/rate_limit.py"
      to: "backend/core/exceptions.py"
      via: "RateLimitException"
      pattern: "RateLimitException"
    - from: "backend/api/errors.py"
      to: "backend/core/exceptions.py"
      via: "BaseAppException"
      pattern: "BaseAppException"
---

<objective>
Build the structured exception layer and a fail-closed Redis rate limiter so infra issues surface as typed errors.
Purpose: keep the FastAPI surface predictable and safe even when Redis or downstream services fail.
Output: `backend/core/exceptions.py`, circuit breaker rate limiter, and updated API error handler that shares the new error contract.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md
@.planning/phases/01-infrastructure-hardening/01-RESEARCH.md
@backend/core/exceptions.py
@backend/core/rate_limit.py
@backend/api/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed application exceptions</name>
  <files>backend/core/exceptions.py</files>
  <action>Create `BaseAppException` (inherits FastAPI `HTTPException`) that captures an `error_code`, `message`, `details`, and optional headers, then derive `RateLimitException`, `ServiceUnavailableException`, and `DatabaseException` that set status codes, codes, and `Retry-After` when applicable. Provide helper `to_payload()` if needed by handlers and keep logging-friendly reprs.</action>
  <verify>Run `python -m pytest backend/tests/test_market_api.py -k error` (or a broader pytest run if no error-specific tag) to validate the module imports cleanly.</verify>
  <done>The file defines the requested exception hierarchy, exposes header support, and integrates docstrings/tests so other modules can import `RateLimitException`/`ServiceUnavailableException`. </done>
</task>

<task type="auto">
  <name>Task 2: Fail-closed rate limiter</name>
  <files>backend/core/rate_limit.py</files>
  <action>Wrap the Redis connection in a `pybreaker` `CircuitBreaker`, call it when establishing the client, and update `check_rate_limit` to raise `RateLimitException` when Redis is reachable but throttled and `ServiceUnavailableException` (with `Retry-After`) when Redis is unavailable. Remove the current fail-open return value so callers only see structured failures, and log precise context for monitoring.</action>
  <verify>Re-run `python -m pytest backend/tests/test_market_api.py -k rate` to ensure the new failure paths don’t break executor tests.</verify>
  <done>Any Redis outage or request spike leads to structured exceptions (503/429) with appropriate headers, and the rate limiter never falls back to returning True.</done>
</task>

<task type="auto">
  <name>Task 3: Wire exceptions into the error handler</name>
  <files>backend/api/errors.py</files>
  <action>Add a handler for `BaseAppException` that logs via `log_system_error`, reuses `build_error_payload`, and includes any headers from the exception (e.g., `Retry-After`). Ensure generic handlers still wrap exceptions in the RFC9457-like envelope, and that validation errors continue returning structured responses.</action>
  <verify>Run `python -m pytest backend/tests/test_market_api.py -k error` to confirm the handler registration compiles and the new responses don’t break existing flows.</verify>
  <done>`api.errors` now recognizes `BaseAppException`, returns the typed payload + headers, and still logs the contextual details defined in research.</done>
</task>

</tasks>

<verification>
PSR: `python -m pytest backend/tests/test_market_api.py -k rate` (covers the new rate limiter) and `python -m pytest backend/tests/test_market_api.py -k error` after wiring the handlers.
</verification>

<success_criteria>
- Circuit breaker rate limiter raises `RateLimitException`/`ServiceUnavailableException` and never returns a True result when Redis fails.
- `api.errors` returns the structured payload plus headers (Retry-After) for every `BaseAppException`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-02-SUMMARY.md`
</output>
