---
phase: 01
plan: 09
type: execute
wave: 3
depends_on:
  - 01-07
  - 01-08
files_modified:
  - backend/api/alerts.py
  - backend/api/market.py
  - backend/api/strategies.py
  - backend/api/risk.py
  - backend/api/ai.py
  - backend/api/export.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Exception handlers provide specific error types"
    - "Stack traces logged for errors"
  artifacts:
    - path: "backend/api/alerts.py"
      provides: "Structured error handling"
      contains: "exc_info=True"
  key_links:
    - from: "backend/api/alerts.py"
      to: "backend/core/exceptions.py"
      via: "import"
---

<objective>
Replace bare `except Exception` blocks with specific exception handling across all API endpoints. Ensure that exceptions are logged with stack traces (`exc_info=True`) and that appropriate HTTP exceptions are raised with structured details.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/core/exceptions.py
@backend/api/alerts.py
@backend/api/market.py
@backend/api/strategies.py
@backend/api/risk.py
@backend/api/ai.py
@backend/api/export.py
</context>

<tasks>

<task type="auto">
  <name>Structure Exceptions in Alerts and Market</name>
  <files>backend/api/alerts.py, backend/api/market.py</files>
  <action>
    1. Import specific exceptions (e.g., `DatabaseException`, `ServiceUnavailableException`) from `core.exceptions` or `sqlalchemy.exc`.
    2. Replace generic `except Exception` blocks with specific catches where possible (e.g., `SQLAlchemyError`).
    3. For remaining `except Exception` blocks (last resort), ensure `logger.exception` or `logger.error(..., exc_info=True)` is used.
    4. Raise `HTTPException` with clear details, but do NOT leak raw exception strings to the client unless sanitized.
  </action>
  <verify>grep "exc_info=True" backend/api/alerts.py</verify>
  <done>Alerts and Market APIs have structured logging and error handling</done>
</task>

<task type="auto">
  <name>Structure Exceptions in Strategies and Risk</name>
  <files>backend/api/strategies.py, backend/api/risk.py</files>
  <action>
    Same as above:
    1. Import specific exceptions.
    2. Replace bare excepts with specific types.
    3. Ensure stack trace logging.
  </action>
  <verify>grep "exc_info=True" backend/api/strategies.py</verify>
  <done>Strategies and Risk APIs have structured logging and error handling</done>
</task>

<task type="auto">
  <name>Structure Exceptions in AI and Export</name>
  <files>backend/api/ai.py, backend/api/export.py</files>
  <action>
    Same as above. Focus on external service failures (OpenAI, file system) in these modules.
  </action>
  <verify>grep "exc_info=True" backend/api/ai.py</verify>
  <done>AI and Export APIs have structured logging and error handling</done>
</task>

</tasks>

<verification>
grep "exc_info=True" backend/api/market.py
grep "exc_info=True" backend/api/strategies.py
</verification>

<success_criteria>
- No silent failures (all exceptions logged)
- Stack traces available in logs
- Clients receive standard HTTP error responses
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-09-SUMMARY.md`
</output>
