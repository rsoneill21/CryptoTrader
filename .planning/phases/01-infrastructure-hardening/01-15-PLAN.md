---
phase: 01-infrastructure-hardening
plan: 15
type: execute
wave: 2
depends_on: ["01-14"]
files_modified:
  - backend/api/market.py
  - backend/api/strategies.py
  - backend/api/trades.py
  - backend/api/system.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Exception handlers provide specific error types"
    - "Stack traces are logged with exc_info=True"
    - "No bare except Exception blocks bypass structured error handling"
  artifacts:
    - path: "backend/api/market.py"
      provides: "Typed exception handling for market endpoints"
      contains: "ServiceUnavailableException"
    - path: "backend/api/strategies.py"
      provides: "Typed exception handling for strategy endpoints"
      contains: "DatabaseException"
    - path: "backend/api/trades.py"
      provides: "Typed exception handling for trade endpoints"
      contains: "DatabaseException"
    - path: "backend/api/system.py"
      provides: "Typed exception handling for system endpoints"
      contains: "exc_info=True"
  key_links:
    - from: "backend/api/trades.py"
      to: "backend/core/exceptions.py"
      via: "DatabaseException import"
      pattern: "from core.exceptions import.*DatabaseException"
---

<objective>
Replace bare `except Exception` blocks with typed exceptions and proper logging across API files.

Purpose: Large portions of the API still rely on bare `except Exception` blocks that swallow context and bypass the structured exception handling established in 01-09. This hurts observability, debugging, and error categorization.

Output: All catch-all blocks converted to specific exception types (DatabaseException, ServiceUnavailableException) with exc_info logging.
</objective>

<execution_context>
@/home/packnation82/.claude/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-hardening/01-09-SUMMARY.md

@backend/api/market.py
@backend/api/strategies.py
@backend/api/trades.py
@backend/api/system.py
@backend/core/exceptions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix exception handling in market.py</name>
  <files>backend/api/market.py</files>
  <action>
    Review market.py for bare `except Exception` blocks and convert to typed exceptions.

    Lines 498-532 contain market analysis endpoint with bare catches:

    1. Line 498: `except Exception as exc:` for technical data
       - This is acceptable as a best-effort summary - just ensure exc_info=True is used
       - Keep as HTTPException but ensure logging has exc_info=True

    2. Lines 516, 522, 531: `except Exception as exc:` for indicator/insights/sentiment
       - These are also best-effort (non-critical) - acceptable to continue on failure
       - Ensure all logger.warning calls have exc_info=True (they already do)

    For market.py, the bare catches are intentional "best effort" patterns where
    failure is logged but doesn't stop the response. Verify each has exc_info=True.

    Add import if not present: `from core.exceptions import ServiceUnavailableException`

    If any catch blocks swallow errors without logging or re-raising, fix them:
    - Add logger.error/warning with exc_info=True
    - Or convert to typed exception and re-raise
  </action>
  <verify>
    1. grep -n "exc_info=True" backend/api/market.py shows logging in all exception handlers
    2. No silent exception swallowing (every except block either logs or re-raises)
  </verify>
  <done>Market.py exception handlers use exc_info=True for proper stack trace logging</done>
</task>

<task type="auto">
  <name>Task 2: Fix exception handling in strategies.py</name>
  <files>backend/api/strategies.py</files>
  <action>
    Review strategies.py for bare `except Exception` blocks (lines 491-1012 per VERIFICATION.md).

    1. Lines 491-494: Strategy AI suggestion failure
       - Currently: `except Exception as exc: logger.warning(..., exc_info=True)`
       - This is acceptable - AI suggestion is non-critical, failure returns None proposal

    2. Lines 521-524: Risk context collection failure
       - Currently: `except Exception as exc: logger.warning(..., exc_info=True)`
       - Acceptable - risk context is enhancement, not required

    3. Lines 539-540: Strategy suggestion generation failure
       - Currently: `except Exception as exc: logger.error(..., exc_info=True)`
       - Should convert to DatabaseException or ServiceUnavailableException if DB-related

    4. Lines 715-720: GitHub import service failure
       - Currently raises ServiceUnavailableException - GOOD, already typed

    5. Line 1009-1015: Simulate strategy catch-all
       - Currently: `except Exception as exc:` raises HTTPException(500)
       - Should raise ServiceUnavailableException for better categorization

    For each bare `except Exception`:
    - If it's SQLAlchemy-related: raise DatabaseException
    - If it's external service: raise ServiceUnavailableException
    - If it's best-effort (non-critical): keep warning with exc_info=True

    Ensure import exists: `from core.exceptions import DatabaseException, ServiceUnavailableException`
  </action>
  <verify>
    1. grep -n "except Exception" backend/api/strategies.py - review each is intentional
    2. All exception blocks have exc_info=True or raise typed exception
  </verify>
  <done>Strategies.py uses typed exceptions or explicit exc_info logging for all error paths</done>
</task>

<task type="auto">
  <name>Task 3: Fix exception handling in trades.py</name>
  <files>backend/api/trades.py</files>
  <action>
    Review trades.py for bare `except Exception` blocks (lines 206-921 per VERIFICATION.md).

    Located bare catches at approximately:
    - Line 206: create manual trade commit failure
    - Line 248: create system trade commit failure
    - Line 313: trade list fetch failure
    - Line 430: active trades fetch failure
    - Line 501: close trade commit failure
    - Line 571: adjust trade commit failure
    - Line 684: toggle ai_managed commit failure
    - Line 757: add to position commit failure
    - Line 831: list trade orders commit
    - Line 889: cancel order commit failure
    - Line 921: get order status sync failure

    For each bare `except Exception as exc:`:

    1. If it's a database commit/query failure, replace with:
       ```python
       except SQLAlchemyError as exc:
           await db.rollback()
           logger.error("...", exc_info=True)
           raise DatabaseException(
               message="...",
               details={"operation": "...", ...}
           ) from exc
       ```

    2. For external service failures (Kraken API), replace with:
       ```python
       except Exception as exc:
           logger.error("...", exc_info=True)
           raise ServiceUnavailableException(
               service="kraken",
               details={"error": str(exc)}
           ) from exc
       ```

    3. Ensure import exists:
       ```python
       from core.exceptions import DatabaseException, ServiceUnavailableException
       from sqlalchemy.exc import SQLAlchemyError
       ```

    Key pattern changes:
    - `except Exception as exc:` -> `except SQLAlchemyError as exc:` for DB operations
    - `raise HTTPException(status_code=500, detail=...)` -> `raise DatabaseException(...)`
    - All logger.error calls must have exc_info=True
  </action>
  <verify>
    1. grep -c "except Exception" backend/api/trades.py shows reduced count
    2. grep -c "DatabaseException" backend/api/trades.py shows typed exceptions used
    3. grep -c "exc_info=True" backend/api/trades.py shows proper logging
  </verify>
  <done>Trades.py uses DatabaseException for DB errors and ServiceUnavailableException for external service errors</done>
</task>

<task type="auto">
  <name>Task 4: Fix exception handling in system.py</name>
  <files>backend/api/system.py</files>
  <action>
    Review system.py for bare `except Exception` blocks (lines 141-348 per VERIFICATION.md).

    Located bare catches:
    - Line 141: health_check Kraken error handling
    - Line 184: connection_status Kraken error
    - Line 292, 348, etc.: backup operation errors

    1. For Kraken connectivity checks (lines 141, 184):
       - These already handle KrakenAPIError specifically
       - The bare `except Exception` is for unexpected errors - acceptable
       - Ensure logging has exc_info=True

    2. For backup operations (lines 292, 348, etc.):
       - Currently raises HTTPException(500)
       - Already logs with logger.exception which includes stack trace
       - Consider converting to ServiceUnavailableException for consistency

    Pattern:
    ```python
    except Exception:
        logger.exception("Unexpected error while creating database backup")
        raise ServiceUnavailableException(
            service="database_backup",
            details={"operation": "create_backup"}
        )
    ```

    Note: logger.exception() automatically includes exc_info, but explicit
    logger.error(..., exc_info=True) is also acceptable.

    Ensure import: `from core.exceptions import ServiceUnavailableException`
  </action>
  <verify>
    1. All exception handlers in system.py either use logger.exception or exc_info=True
    2. grep "ServiceUnavailableException" backend/api/system.py shows typed exceptions for service failures
  </verify>
  <done>System.py uses proper exception logging and typed exceptions for service errors</done>
</task>

</tasks>

<verification>
1. All files have proper exception handling:
   - grep -c "exc_info=True" backend/api/market.py backend/api/strategies.py backend/api/trades.py backend/api/system.py
   - Each file should show multiple occurrences
2. Typed exceptions are used:
   - grep -c "DatabaseException\|ServiceUnavailableException" backend/api/trades.py shows usage
3. No silent exception swallowing:
   - Every except block either logs with exc_info/exception or raises a typed exception
4. Server starts: cd backend && uvicorn main:app --reload
</verification>

<success_criteria>
- All bare `except Exception` blocks either:
  - Log with exc_info=True and continue (for best-effort operations)
  - Raise typed exceptions (DatabaseException, ServiceUnavailableException)
- Stack traces are captured via exc_info=True or logger.exception()
- Error categorization is improved for observability
- Server functions normally after changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-15-SUMMARY.md`
</output>
