---
phase: 01-infrastructure-hardening
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/system.py
  - backend/core/exceptions.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "System health endpoints raise typed errors when dependencies fail"
    - "Unexpected Kraken/Redis outages produce structured logs with stack traces"
  artifacts:
    - path: "backend/api/system.py"
      provides: "Typed ServiceUnavailableException responses for /health and /connection-status"
      contains: "ServiceUnavailableException"
    - path: "backend/core/exceptions.py"
      provides: "Consistent error envelope helpers"
      contains: "class ServiceUnavailableException"
  key_links:
    - from: "backend/api/system.py"
      to: "backend/core/exceptions.py"
      via: "ServiceUnavailableException import"
      pattern: "from core.exceptions import.*ServiceUnavailableException"
---

<objective>
Surface structured exceptions for `/api/system/health` and `/api/system/connection-status` so unexpected Kraken/Redis outages log stack traces and bubble through the global error handler.

Purpose: Verification shows system endpoints still swallow bare `Exception`, returning inline strings. We need typed `ServiceUnavailableException` responses with `logger.exception(..., exc_info=True)` so monitoring and clients see real failures.

Output: `/health` and `/connection-status` log unexpected errors with stack traces and re-raise `ServiceUnavailableException`, reusing the shared error envelope.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-hardening/01-VERIFICATION.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md

@backend/api/system.py
@backend/core/exceptions.py
@backend/core/rate_limit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument system health endpoints with structured logging</name>
  <files>backend/api/system.py</files>
  <action>
    - Focus on `/health` and `/connection-status` handlers (approx. lines 133-188).
    - Replace bare `except Exception` blocks with:
      1. `logger.exception("message", extra={...})` or `logger.error(..., exc_info=True)` capturing Kraken/Redis call context.
      2. Raising `ServiceUnavailableException(service="kraken", details={...})` (or `service="redis"`) so FastAPI emits a typed error envelope.
    - Preserve existing success payload shape when dependencies are healthy.
    - Ensure the shared error envelope still complies with `{"error": {"type": ...}}` contract.
  </action>
  <verify>
    1. `rg -n "except Exception" backend/api/system.py` shows no bare catches in `/health` or `/connection-status`.
    2. `rg -n "ServiceUnavailableException" backend/api/system.py` shows usage in both handlers.
  </verify>
  <done>Both system endpoints log unexpected errors with stack traces and raise typed exceptions</done>
</task>

<task type="auto">
  <name>Task 2: Ensure ServiceUnavailableException payload covers system diagnostics</name>
  <files>backend/api/system.py, backend/core/exceptions.py</files>
  <action>
    - Extend the raised exception details to include endpoint, dependency, and original error string so observability gains context.
    - If needed, add helper to `ServiceUnavailableException` (core/exceptions.py) to default `details` keys like `{"endpoint": "health", "dependency": "kraken"}`.
    - Confirm logger statements include `exc_info=True` or use `logger.exception` for stack traces.
    - Update docstrings/comments to explain fail-closed behavior for these endpoints.
  </action>
  <verify>
    1. `rg -n "exc_info=True" backend/api/system.py` includes new log statements.
    2. Run `pytest backend/tests/api/test_system.py -k health --maxfail=1` (if tests exist) or `uvicorn main:app --reload` to ensure imports remain valid.
  </verify>
  <done>Typed exceptions carry dependency context and imports compile cleanly</done>
</task>

</tasks>

<verification>
1. `rg -n "ServiceUnavailableException" backend/api/system.py`
2. `rg -n "exc_info=True" backend/api/system.py`
3. `uvicorn main:app --reload` (backend/) to confirm module loads
</verification>

<success_criteria>
- `/api/system/health` and `/api/system/connection-status` raise `ServiceUnavailableException` for unexpected failures
- Stack traces logged via `logger.exception` or `exc_info=True`
- Error envelope matches locked structure with dependency details
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-16-SUMMARY.md`
</output>
