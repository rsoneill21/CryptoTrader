---
phase: 01-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/database.py
  - backend/api/trades.py
autonomous: true
must_haves:
  truths:
    - "Active trade endpoints execute with AsyncSession so database work never blocks the FastAPI event loop."
    - "Database module exposes both async and sync session factories so follow-on plans can reuse them without racing the legacy helpers."
  artifacts:
    - path: "backend/db/database.py"
      provides: "async_engine, AsyncSessionLocal, and get_async_db along with backwards-compatible helpers."
    - path: "backend/api/trades.py"
      provides: "Create/list/close trade endpoints that await AsyncSession commands and return structured payloads."
  key_links:
    - from: "backend/api/trades.py"
      to: "backend/db/database.py#get_async_db"
      via: "Depends(get_async_db)"
      pattern: "Depends\(get_async_db\)"
---

<objective>
Upgrade the database plumbing so the most critical trade APIs run on an AsyncSession pipeline.
Purpose: remove blocking database calls from FastAPI routes and give every future plan a reliable async dependency.
Output: async engine/session factory in `backend/db/database.py` plus trades APIs rewritten to await AsyncSession operations.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md
@.planning/phases/01-infrastructure-hardening/01-RESEARCH.md
@backend/db/database.py
@backend/api/trades.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build async session factory</name>
  <files>backend/db/database.py</files>
  <action>Create an `AsyncEngine` via `create_async_engine`, swap `SessionLocal` for an `async_sessionmaker`, and add a `get_async_db` dependency that yields `AsyncSession`. Keep the existing synchronous `SessionLocal`/`get_db` helpers so synchronous consumers (logging, legacy scripts) keep working, but route all new async API logic through the new factory. Ensure `init_db()` still applies Alembic revisions (use `async_engine.begin()` or `run_sync`) and that helper utilities (e.g., `log_system_error`) can still obtain a sync session without blocking the event loop.</action>
  <verify>Run `python backend/init_db.py` to prove the async engine boots and migrations apply, then execute `python -m pytest backend/tests/test_market_api.py -k trades` so the new dependency doesn’t break existing tests.</verify>
  <done>`backend/db/database.py` exports `async_engine`, `AsyncSessionLocal`, and `get_async_db`, `init_db()` uses the async engine safely, and the legacy sync helpers still work without change.</done>
</task>

<task type="auto">
  <name>Task 2: Convert trades API to async sessions</name>
  <files>backend/api/trades.py</files>
  <action>Switch every FastAPI trades endpoint (`create_manual_trade`, `create_system_trade`, `list_active_trades`, `close_trade`) to depend on `AsyncSession` from `backend/db/database.get_async_db`. Replace sync `db.query`/`db.commit` flows with `await db.execute(select(...))`, `await db.commit()`, and `await db.refresh(trade)` plus `result.scalars()`/`.scalar_one_or_none()`. Preserve logging, error details, and market price fetches, but ensure all database calls are awaited so the event loop never blocks.</action>
  <verify>Repeat `python -m pytest backend/tests/test_market_api.py -k trades` to confirm the async changes still satisfy the trade-flow tests.</verify>
  <done>Trades API handlers await AsyncSession operations, use `select`/`scalars()` instead of sync queries, and the pytest trade suite passes.</done>
</task>

</tasks>

<verification>
Confirm the async session factory boots (`backend/init_db.py`) and the `backend/tests/test_market_api.py -k trades` target passes after both tasks.
</verification>

<success_criteria>
- `backend/db/database.py` exposes `AsyncSessionLocal`/`get_async_db` while keeping synchronous helpers intact.
- `backend/api/trades.py` awaits AsyncSession calls and this change doesn’t break the targeted pytest suite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-01-SUMMARY.md`
</output>
