---
phase: 01-infrastructure-hardening
plan: 10
type: execute
wave: 1
depends_on: []
files_modified: [backend/api/trades.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "POST /api/trades returns 201 with trade data including orders array"
    - "POST /api/trades/system returns 201 with trade data including orders array"
    - "Trade serialization accesses trade.orders without DetachedInstanceError"
  artifacts:
    - path: "backend/api/trades.py"
      provides: "Trade creation endpoints with eager loaded orders"
      min_lines: 260
      functions: ["create_manual_trade", "create_system_trade"]
  key_links:
    - from: "create_manual_trade (line 206)"
      to: "trade.orders"
      via: "selectinload before _serialize_trade"
      pattern: "selectinload\\(Trade\\.orders\\)"
    - from: "create_system_trade (line 247)"
      to: "trade.orders"
      via: "selectinload before _serialize_trade"
      pattern: "selectinload\\(Trade\\.orders\\)"
---

<objective>
Fix POST /api/trades and POST /api/trades/system 500 errors by eagerly loading Trade.orders relationship before serialization.

Purpose: Paper trading positions can be created via API so state changes can be validated across backend restarts.
Output: Both POST endpoints return 201 with complete trade data including orders array.
</objective>

<execution_context>
@/home/packnation82/.claude/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/packnation82/projects/CryptoTrader/.planning/PROJECT.md
@/home/packnation82/projects/CryptoTrader/.planning/ROADMAP.md
@/home/packnation82/projects/CryptoTrader/.planning/STATE.md
@/home/packnation82/projects/CryptoTrader/.planning/phases/01-infrastructure-hardening/01-UAT.md
@/home/packnation82/projects/CryptoTrader/backend/api/trades.py
</context>

<tasks>

<task type="auto">
  <name>Reload trade with orders relationship before serialization</name>
  <files>backend/api/trades.py</files>
  <action>
After db.refresh(trade) in both create_manual_trade (line 205) and create_system_trade (line 247), add one line to reload the trade with selectinload(Trade.orders):

```python
trade = await db.scalar(select(Trade).where(Trade.id == trade.id).options(selectinload(Trade.orders)))
```

This ensures trade.orders is loaded before _serialize_trade accesses it on line 399.

DO NOT change the _serialize_trade function - it already correctly iterates trade.orders.
DO NOT add try/except around the reload - the existing exception handler already catches and logs DB errors.

Why this approach: AsyncSession doesn't eagerly load relationships by default. The refresh() call reloads scalar attributes but not relationships. Must explicitly reload with selectinload to populate trade.orders in the session before serialization.
  </action>
  <verify>
Run `curl -X POST http://localhost:8000/api/trades -H "Content-Type: application/json" -d '{"symbol":"BTC/USD","side":"buy","quantity":0.1,"is_paper":true}'` with valid auth token and verify response is 201 with trade object containing orders array (may be empty).
  </verify>
  <done>POST /api/trades and POST /api/trades/system both return 201 with ActiveTradeResponse including orders array, no 500 errors.</done>
</task>

</tasks>

<verification>
- [ ] POST /api/trades returns 201 (not 500)
- [ ] POST /api/trades/system returns 201 (not 500)
- [ ] Response includes orders array
- [ ] No DetachedInstanceError in logs
</verification>

<success_criteria>
Paper trading trades can be created via API. Both POST endpoints return 201 with complete trade data. Test 3 from UAT can now proceed to validate state persistence across restarts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-10-SUMMARY.md`
</output>
