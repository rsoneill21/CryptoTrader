---
phase: 01
plan: 08
type: execute
wave: 2
depends_on:
  - 01-07
files_modified:
  - backend/api/strategies.py
  - backend/api/trades.py
  - backend/api/alerts.py
  - backend/core/pagination.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "List endpoints return paginated results"
    - "Pagination logic is consistent across endpoints"
  artifacts:
    - path: "backend/core/pagination.py"
      provides: "Cursor pagination utilities"
      exports: ["encode_cursor", "decode_cursor", "apply_cursor_pagination"]
    - path: "backend/api/strategies.py"
      provides: "Paginated strategies list"
      contains: "next_cursor"
    - path: "backend/api/trades.py"
      provides: "Paginated trades list"
      contains: "next_cursor"
  key_links:
    - from: "backend/api/strategies.py"
      to: "backend/core/pagination.py"
      via: "import"
---

<objective>
Implement consistent cursor-based pagination for `strategies` and `trades` list endpoints. Extract the pagination logic from `alerts.py` into a shared utility to ensure maintainability and consistency.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/api/alerts.py
@backend/api/strategies.py
@backend/api/trades.py
</context>

<tasks>

<task type="auto">
  <name>Extract Pagination Utilities</name>
  <files>backend/core/pagination.py, backend/api/alerts.py</files>
  <action>
    1. Create `backend/core/pagination.py`.
    2. Move `encode_cursor`, `decode_cursor`, `DEFAULT_PAGE_LIMIT`, `MAX_PAGE_LIMIT` from `backend/api/alerts.py` to this new file.
    3. Update `backend/api/alerts.py` to import these utilities.
    4. (Optional) Create a helper `apply_cursor_pagination` in `core/pagination.py` that takes a query, cursor, and model, and applies the filtering logic (created_at/id) to reduce code duplication.
  </action>
  <verify>test -f backend/core/pagination.py</verify>
  <done>Pagination logic extracted and alerts.py refactored</done>
</task>

<task type="auto">
  <name>Paginate Strategies List</name>
  <files>backend/api/strategies.py</files>
  <action>
    Update `list_strategies`:
    1. Accept `cursor`, `limit` query parameters.
    2. Use `decode_cursor` to parse cursor.
    3. Update query to apply cursor filtering (created_at/id).
    4. Order by `created_at DESC, id DESC`.
    5. Fetch limit+1 to detect `has_more`.
    6. Return `next_cursor`, `has_more` in response model.
    7. Define `StrategyListResponse` model.
  </action>
  <verify>grep "StrategyListResponse" backend/api/strategies.py</verify>
  <done>Strategies endpoint supports cursor pagination</done>
</task>

<task type="auto">
  <name>Paginate Trades List</name>
  <files>backend/api/trades.py</files>
  <action>
    Update `list_trades`:
    1. Accept `cursor`, `limit` query parameters.
    2. Use `decode_cursor` to parse cursor.
    3. Update query to apply cursor filtering.
    4. Order by `timestamp DESC, id DESC` (or created_at if timestamp not unique/present, but trades usually have execution timestamp).
    5. Return `TradeListResponse` with cursor fields.
  </action>
  <verify>grep "TradeListResponse" backend/api/trades.py</verify>
  <done>Trades endpoint supports cursor pagination</done>
</task>

</tasks>

<verification>
grep "encode_cursor" backend/core/pagination.py
grep "next_cursor" backend/api/strategies.py
grep "next_cursor" backend/api/trades.py
</verification>

<success_criteria>
- Common pagination logic resides in `core/pagination.py`
- Strategies and Trades list endpoints accept `cursor` and return `next_cursor`
- Alerts endpoint still works (refactored to use shared utils)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-08-SUMMARY.md`
</output>
