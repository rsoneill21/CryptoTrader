---
phase: 01-infrastructure-hardening
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/market.py
  - backend/core/exceptions.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Market analysis surfaces upstream outages via typed errors"
    - "Indicator/sentiment collectors never swallow exceptions silently"
  artifacts:
    - path: "backend/api/market.py"
      provides: "Typed ServiceUnavailableException handling in /analysis"
      contains: "ServiceUnavailableException"
    - path: "backend/core/exceptions.py"
      provides: "Shared error envelope for indicator services"
      contains: "class ServiceUnavailableException"
  key_links:
    - from: "backend/api/market.py"
      to: "backend/core/exceptions.py"
      via: "ServiceUnavailableException import"
      pattern: "from core.exceptions import.*ServiceUnavailableException"
---

<objective>
Ensure `/api/market/analysis` does not return HTTP 200 when downstream indicator or sentiment collectors fail by wrapping those calls in typed `ServiceUnavailableException` blocks with stack-trace logging.

Purpose: Verification shows best-effort warnings still let responses succeed, hiding outages. We need typed exceptions and observability so dashboards and monitors alert on failure.

Output: Indicator/insight/sentiment sections log stack traces and raise `ServiceUnavailableException` (or provide explicit degraded-mode fields) instead of silently omitting data.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-hardening/01-VERIFICATION.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md

@backend/api/market.py
@backend/core/exceptions.py
@backend/services/market_insights_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace warning-only blocks with typed exceptions</name>
  <files>backend/api/market.py</files>
  <action>
    - Inspect `/analysis` endpoint around lines 498-534.
    - For each downstream call (technical summary, indicator groups, insights, sentiment):
      1. Keep success behavior unchanged.
      2. Wrap each call in try/except; when unexpected `Exception` occurs, log via `logger.exception(...)` and raise `ServiceUnavailableException(service="market_analysis", details={"segment": "sentiment", ...})`.
    - If partial degradation should still return 200, gate it behind a `degraded` flag so clients know data is incomplete; otherwise fail the request entirely.
  </action>
  <verify>
    1. `rg -n "ServiceUnavailableException" backend/api/market.py` shows usage in every downstream block.
    2. `rg -n "logger\.exception" backend/api/market.py` includes indicator/sentiment sections.
  </verify>
  <done>Market analysis raises typed errors (or explicit degraded flag) instead of silent warnings</done>
</task>

<task type="auto">
  <name>Task 2: Ensure degraded-mode responses are explicit when tolerated</name>
  <files>backend/api/market.py, backend/core/exceptions.py</files>
  <action>
    - If product requirements prefer partial responses, include a `"degraded": true` flag plus `"missing_segments": [...]` to satisfy observability while still returning 200; document the behavior in inline comments.
    - Otherwise, propagate `ServiceUnavailableException` so FastAPI returns 503/structured payload.
    - Extend exception details, if needed, to include the failing service name (e.g., `"dependency": "sentiment"`).
  </action>
  <verify>
    1. Added unit coverage (preferred) or manual `pytest backend/tests/api/test_market.py -k analysis` to ensure contract updates are captured.
    2. `rg -n "degraded" backend/api/market.py` (if using degraded mode) shows explicit signaling.
  </verify>
  <done>Clients can detect degraded analysis or receive typed errors; no silent failure remains</done>
</task>

</tasks>

<verification>
1. `rg -n "ServiceUnavailableException" backend/api/market.py`
2. `rg -n "logger\.exception" backend/api/market.py`
3. `pytest backend/tests/api/test_market.py -k analysis --maxfail=1`
</verification>

<success_criteria>
- Indicator/sentiment collectors either succeed or raise `ServiceUnavailableException`
- Stack traces captured for every failure path
- Clients receive structured error or explicit degraded flag
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-17-SUMMARY.md`
</output>
