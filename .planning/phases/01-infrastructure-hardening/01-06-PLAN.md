---
phase: 01
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Server restart preserves paper trading state"
    - "Paper trading engine initializes on startup"
  artifacts:
    - path: "backend/main.py"
      provides: "Application lifespan with paper trading hooks"
      contains: "initialize_paper_trading_engine"
  key_links:
    - from: "backend/main.py"
      to: "backend/services/paper_trading_service.py"
      via: "import"
---

<objective>
Wire the paper trading engine initialization and shutdown hooks into the FastAPI application lifespan. This ensures that paper trading state (positions, cash, P&L) is loaded from the database on startup and persisted on shutdown, preventing state loss.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/main.py
@backend/services/paper_trading_service.py
</context>

<tasks>

<task type="auto">
  <name>Wire Paper Trading Lifecycle Hooks</name>
  <files>backend/main.py</files>
  <action>
    Modify `lifespan` function in `backend/main.py`:
    1. Import `initialize_paper_trading_engine` and `shutdown_paper_trading_engine` from `services.paper_trading_service`.
    2. In the startup phase (before `yield`), add `await initialize_paper_trading_engine()`.
    3. In the shutdown phase (after `yield`), add `await shutdown_paper_trading_engine()`.
    
    Ensure these are awaited as they are async functions.
  </action>
  <verify>
    Check `backend/main.py` for the imports and the `await` calls within `lifespan`.
  </verify>
  <done>
    `initialize_paper_trading_engine` is called on startup.
    `shutdown_paper_trading_engine` is called on shutdown.
  </done>
</task>

</tasks>

<verification>
grep "initialize_paper_trading_engine" backend/main.py
grep "shutdown_paper_trading_engine" backend/main.py
</verification>

<success_criteria>
- Paper trading engine hooks are connected to FastAPI lifespan
- State persistence logic (already implemented in service) is actually triggered
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-06-SUMMARY.md`
</output>
