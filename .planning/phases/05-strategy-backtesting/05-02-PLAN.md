---
phase: 05-strategy-backtesting
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [backend/services/backtest_service.py, backend/core/strategy_evaluator.py, backend/api/backtests.py]
autonomous: false
must_haves:
  truths:
    - "Backtest engine correctly iterates through historical data without lookahead bias."
    - "Strategy evaluator supports basic technical indicator conditions (RSI, MA)."
  artifacts:
    - path: "backend/core/strategy_evaluator.py"
      provides: "Rule-based signal generation"
    - path: "backend/services/backtest_service.py"
      provides: "Historical simulation loop"
---

<objective>
Implement the backtest execution engine and strategy rule evaluator.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement StrategyEvaluator in backend/core/strategy_evaluator.py</name>
  <files>backend/core/strategy_evaluator.py</files>
  <action>
    Create a class that can evaluate JSON-defined rules against a series of candles:
    - Support basic indicators: SMA, EMA, RSI.
    - Support operators: `>`, `<`, `==`, `>=`, `<=`.
    - Support logic: `AND`, `OR`.
    - Method `evaluate(candles: List[Candle], rules: Dict) -> Signal`
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement BacktestService in backend/services/backtest_service.py</name>
  <files>backend/services/backtest_service.py</files>
  <action>
    Create a service to run the simulation:
    - Fetch historical candles from `MarketData` for the given range.
    - Iterate through candles one by one (simulating time progression).
    - For each step, call `StrategyEvaluator` to get signals.
    - Feed signals into a non-persisting `PaperTradingEngine`.
    - Capture metrics: equity curve, trade list, final P&L, drawdown, win rate.
    - Update `BacktestRun` record with results and status.
  </action>
</task>

<task type="auto">
  <name>Task 3: Wire BacktestService into API</name>
  <files>backend/api/backtests.py</files>
  <action>
    Update `trigger_backtest` endpoint to start the backtest in a background task using the new `BacktestService`.
  </action>
</task>

</tasks>

<verification>
1. Unit tests for `StrategyEvaluator` with various rule sets.
2. Integration test for `BacktestService` using seeded market data.
</verification>
