---
phase: 05-strategy-backtesting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/db/models.py, backend/api/strategies.py, backend/api/backtests.py, backend/main.py]
autonomous: false
must_haves:
  truths:
    - "Backtest results are persisted in the database with detailed metrics."
    - "API allows triggering a backtest for a specific strategy and symbol."
  artifacts:
    - path: "backend/db/models.py"
      provides: "BacktestRun model"
    - path: "backend/api/backtests.py"
      provides: "Backtest management endpoints"
---

<objective>
Establish the database model and API foundation for strategy backtesting.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add BacktestRun model to backend/db/models.py</name>
  <files>backend/db/models.py</files>
  <action>
    Add the `BacktestRun` model to track backtest parameters and outcomes:
    - Fields: `id`, `strategy_id`, `symbol`, `start_date`, `end_date`, `initial_capital`, `final_capital`, `total_pnl`, `max_drawdown`, `win_rate`, `total_trades`, `status` (running, completed, failed), `results_json`, `created_at`.
    - Set up relationship with `Strategy`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create database migration for BacktestRun</name>
  <files>backend/alembic/versions/</files>
  <action>
    Generate and run an Alembic migration to create the `backtest_runs` table.
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement basic Backtest API in backend/api/backtests.py</name>
  <files>backend/api/backtests.py</files>
  <action>
    Create a new API router for backtests:
    - `POST /api/backtests`: Trigger a backtest (returns 202 Accepted with backtest ID).
    - `GET /api/backtests/{backtest_id}`: Fetch backtest status and results.
    - `GET /api/backtests/strategy/{strategy_id}`: List backtests for a strategy.
    Use Pydantic models for request/response validation.
  </action>
</task>

<task type="auto">
  <name>Task 4: Register Backtest API in backend/main.py</name>
  <files>backend/main.py</files>
  <action>
    Include the new backtest router in the FastAPI application.
  </action>
</task>

</tasks>

<verification>
1. `pytest` confirms the new model can be instantiated and persisted.
2. API endpoints respond with correct status codes and schema.
</verification>
