---
phase: 06-advanced-strategy-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/core/tasks.py, backend/services/strategy_service.py, backend/db/models.py, backend/api/strategies.py]
autonomous: false
must_haves:
  truths:
    - "System periodic task identifies strategies with degrading performance (Drawdown > Limit, WinRate < Threshold)."
    - "Degraded strategies are flagged (status='warning' or similar) or Alerts are generated."
    - "AI service is triggered to propose adjustments for flagged strategies."
  artifacts:
    - path: "backend/core/tasks.py"
      provides: "monitor_strategy_health task"
    - path: "backend/services/strategy_service.py"
      provides: "Logic for degradation checks and AI trigger."
---

<objective>
Implement "Self-Healing" capabilities: Monitor strategy health and trigger AI interventions when performance degrades.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Strategy Health Logic</name>
  <files>backend/services/strategy_service.py</files>
  <action>
    Create `check_strategy_health(strategy_id)`:
    - Load recent performance (last N trades).
    - Check against thresholds (e.g., Win Rate < 40%, Drawdown > 15%).
    - Return HealthStatus (OK, DEGRADED, CRITICAL).
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Periodic Health Task</name>
  <files>backend/core/tasks.py</files>
  <action>
    Add `monitor_active_strategies` task (e.g., hourly).
    - Iterate all "live" strategies.
    - Call health check.
    - If DEGRADED:
      1. Create System Alert.
      2. Call `strategy_ai_service.analyze_degradation(strategy)` to get suggestions.
      3. Save suggestion to `Strategy.pending_adjustment`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Strategy Model</name>
  <files>backend/db/models.py</files>
  <action>
    Ensure `Strategy` model has fields for `health_status` and `pending_adjustment_json` (if not already present or suitable).
  </action>
</task>

</tasks>

<verification>
1. Unit test `test_health_check_degradation`: Seed a strategy with losing trades and verify it flags as DEGRADED.
2. Unit test `test_monitor_task`: Run the task and verify it generates an Alert and calls the AI service mock.
</verification>
