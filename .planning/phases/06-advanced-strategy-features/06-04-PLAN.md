---
phase: 06-advanced-strategy-features
plan: 04
type: execute
wave: 2
depends_on: [06-02]
files_modified: [frontend/src/pages/StrategyLab.js, frontend/src/components/StrategyCard.js, frontend/src/services/api.js]
autonomous: false
must_haves:
  truths:
    - "User can promote a paper strategy to live trading."
    - "User can view health warnings for degraded strategies."
    - "User can approve/reject pending AI adjustments."
  artifacts:
    - path: "frontend/src/pages/StrategyLab.js"
      provides: "Dashboard updates for health and lifecycle."
---

<objective>
Update the UI to manage the strategy lifecycle: Promotion (Paper -> Live) and Health/Adjustment management.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement Promotion Flow</name>
  <files>frontend/src/components/StrategyCard.js, frontend/src/pages/StrategyLab.js</files>
  <action>
    - Add "Promote to Live" button (visible if status=paper).
    - Show confirmation modal with key stats (Win Rate, Drawdown).
    - Call `POST /api/strategies/{id}/promote`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Display Health & Adjustments</name>
  <files>frontend/src/components/StrategyCard.js</files>
  <action>
    - Show Health Badge (Green=OK, Yellow=Degraded).
    - If `pending_adjustment` exists, show "Optimization Available" action.
    - Clicking action shows "Before vs After" comparison of parameters.
    - "Apply" updates the strategy; "Discard" clears the pending adjustment.
  </action>
</task>

</tasks>

<verification>
1. Run frontend lint.
2. Manual verify: Promote a strategy. Check visual feedback for "Degraded" strategy (mocked data).
</verification>
