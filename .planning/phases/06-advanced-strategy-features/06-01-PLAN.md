---
phase: 06-advanced-strategy-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/core/strategy_evaluator.py, backend/services/backtest_service.py, backend/agents/orchestrator.py, backend/api/strategies.py]
autonomous: false
must_haves:
  truths:
    - "Strategy rules can specify a 'timeframe' parameter per condition (e.g., '1h', '15m')."
    - "Evaluator correctly aligns multi-timeframe data (e.g., forward-filling higher TF data to match lower TF)."
    - "Backtest engine fetches data for all required timeframes before simulation."
  artifacts:
    - path: "backend/core/strategy_evaluator.py"
      provides: "Updated evaluate() method accepting multi-timeframe data map."
---

<objective>
Upgrade the strategy engine to support multi-timeframe analysis (e.g., "Trade when 1h Trend is Up AND 5m RSI < 30").
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update StrategyEvaluator for Multi-Timeframe Data</name>
  <files>backend/core/strategy_evaluator.py</files>
  <action>
    Refactor `evaluate` signature to accept `data: Dict[str, pd.DataFrame]` instead of single DataFrame.
    - Key is timeframe ("1m", "1h").
    - Logic:
      1. Identify base timeframe (lowest resolution, usually "1m" or strategy default).
      2. Resample/Align higher timeframe indicators to the base index (forward fill).
      3. Evaluate conditions against the aligned dataset.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update BacktestService data fetching</name>
  <files>backend/services/backtest_service.py</files>
  <action>
    - Parse strategy rules to identify ALL required timeframes.
    - Fetch historical data for each timeframe.
    - Pass the dictionary of DataFrames to the updated `StrategyEvaluator`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Orchestrator/Live Execution</name>
  <files>backend/agents/orchestrator.py</files>
  <action>
    - Ensure live execution fetches latest candles for all required timeframes before evaluating.
    - Pass correct data structure to evaluator.
  </action>
</task>

</tasks>

<verification>
1. Create a unit test `test_multi_timeframe_evaluation` in `backend/tests/core/test_strategy_evaluator.py`.
2. Define a rule using "1h" SMA and "5m" RSI.
3. Feed synthetic data and verify alignment (e.g., 1h SMA should remain constant for twelve 5m candles).
</verification>
