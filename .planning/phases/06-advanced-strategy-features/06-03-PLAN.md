---
phase: 06-advanced-strategy-features
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [frontend/src/components/StrategyWizard.js, frontend/src/pages/StrategyLab.js, frontend/src/services/api.js]
autonomous: false
must_haves:
  truths:
    - "User can generate a strategy from a text description via AI."
    - "User can review and edit the generated rules/parameters."
    - "User can save the new strategy to the database."
  artifacts:
    - path: "frontend/src/components/StrategyWizard.js"
      provides: "Multi-step creation flow."
---

<objective>
Build the "Strategy Wizard" UI to allow users to generate, customize, and save strategies using AI.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create StrategyWizard Component</name>
  <files>frontend/src/components/StrategyWizard.js</files>
  <action>
    Implement a step-by-step modal/view:
    - Step 1 (Intent): Text area for "Describe your strategy". Options for "Risk Profile" (Conservative, Aggressive).
    - Step 2 (Generation): Spinner while calling `POST /api/strategies/suggestions`.
    - Step 3 (Review): Display `StrategyProposal` (Thesis, Rules). Allow editing numeric parameters (thresholds).
    - Step 4 (Save): Name the strategy and save.
  </action>
</task>

<task type="auto">
  <name>Task 2: Integrate into StrategyLab</name>
  <files>frontend/src/pages/StrategyLab.js</files>
  <action>
    - Add "New Strategy" button that opens the Wizard.
    - Refresh list on save.
  </action>
</task>

<task type="auto">
  <name>Task 3: Update Frontend API</name>
  <files>frontend/src/services/api.js</files>
  <action>
    - Add methods for `generateStrategy(prompt)` and `saveStrategy(payload)`.
  </action>
</task>

</tasks>

<verification>
1. Run frontend lint.
2. Manual verification: Open Wizard, type "RSI Reversal", verify AI returns structured JSON, verify Save works.
</verification>
