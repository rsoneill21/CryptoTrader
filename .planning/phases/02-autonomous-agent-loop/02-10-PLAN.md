---
phase: 02-autonomous-agent-loop
plan: 10
type: execute
wave: 4
depends_on: ["02-01", "02-05"]
files_modified:
  - backend/agents/base.py
  - backend/agents/manager.py
  - backend/agents/market_analyst.py
  - backend/agents/orchestrator.py
  - backend/agents/trade_executor.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Configured `agent_*_interval_seconds` values pace each agent so sub-minute cadence is observable from the API/UI while agents are running."
    - "Each insight, signal, and executor outcome records a pipeline event so the queue timeline and throughput counters stay populated while work is flowing."
    - "Operators see non-empty pipeline timeline entries and throughput-per-minute numbers from `/api/agents/dashboard` whenever Market Analyst, Orchestrator, and Trade Executor process messages."
  artifacts:
    - path: "backend/agents/base.py"
      provides: "Run-loop pacing via a `run_interval` property that defaults to configured values."
      contains: "await asyncio.sleep(self.run_interval)"
    - path: "backend/agents/manager.py"
      provides: "Agent wiring to the manager plus pipeline recording and throughput counters."
      contains: "record_pipeline_event"
    - path: "backend/agents/market_analyst.py"
      provides: "Insight publisher that logs every insight via `record_pipeline_event`."
      contains: "record_pipeline_event"
    - path: "backend/agents/orchestrator.py"
      provides: "Signal dispatcher that reports each stream publish as a pipeline event."
      contains: "record_pipeline_event"
    - path: "backend/agents/trade_executor.py"
      provides: "Signal consumer that records executions and retries through the manager."
      contains: "record_pipeline_event"
  key_links:
    - from: "backend/agents/market_analyst.py"
      to: "backend/agents/manager.py"
      via: "record_pipeline_event called after publish_reliable(Channels.STREAM_TRADE_SIGNALS)"
      pattern: "record_pipeline_event"
    - from: "backend/agents/orchestrator.py"
      to: "backend/agents/manager.py"
      via: "record_pipeline_event triggered when multistage trade signals hit Redis Streams"
      pattern: "record_pipeline_event"
    - from: "backend/agents/trade_executor.py"
      to: "backend/agents/manager.py"
      via: "record_pipeline_event at signal handling / retry boundaries"
      pattern: "record_pipeline_event"
---

<objective>
Fix the two verification gaps from Phase 02 by honoring the configurable agent cadence and actually populating the pipeline timeline/throughput counters whenever agents publish or consume messages.
Purpose: Agents must look configurable to operators and the dashboard must stop showing the empty timeline that signals the pipeline isn’t being observed.
Output: Updated BaseAgent/AgentManager wiring plus instrumentation in MarketAnalyst, Orchestrator, and TradeExecutor that feeds pipeline events and throughput into the dashboard.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-autonomous-agent-loop/02-CONTEXT.md
@.planning/phases/02-autonomous-agent-loop/02-RESEARCH.md
@.planning/phases/02-autonomous-agent-loop/02-VERIFICATION.md
@backend/agents/base.py
@backend/agents/manager.py
@backend/agents/market_analyst.py
@backend/agents/orchestrator.py
@backend/agents/trade_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flow configured intervals through BaseAgent run loop</name>
  <files>
backend/agents/base.py
backend/agents/manager.py
  </files>
  <action>
1. Add a `run_interval` property (or getter) on `BaseAgent` that returns the configured `_run_interval` (defaulting to the previous 0.01s when no config exists) and ensures `BaseAgent._run_loop` sleeps against that value instead of the hard-coded `0.01`.
2. When `AgentManager` instantiates each agent, assign `agent.agent_manager = self` (or a setter) so every agent can reach back to `record_pipeline_event`.
3. Log the configured interval during startup so ops can see the interval in case the UI requests verification.
  </action>
  <verify>
python - <<'PY'
import inspect
from agents.base import BaseAgent
from agents.manager import AgentManager

source = inspect.getsource(BaseAgent._run_loop)
assert 'await asyncio.sleep(self.run_interval)' in source

manager = AgentManager()
agent = manager.get_agent('market_analyst')
assert getattr(agent, 'agent_manager', None) is manager
print('BaseAgent run loop uses run_interval and agent_manager attached')
PY
  </verify>
  <done>BaseAgent._run_loop sleeps with `self.run_interval` and every agent keeps a reference to its manager so future tasks can call `record_pipeline_event`. The manager logs the configured interval during startup.</done>
</task>

<task type="auto">
  <name>Task 2: Let each agent’s run routine respect the configured interval</name>
  <files>
backend/agents/market_analyst.py
backend/agents/orchestrator.py
backend/agents/trade_executor.py
  </files>
  <action>
1. Replace the hard-coded `await asyncio.sleep(0.1)` at the end of each `run()` (MarketAnalyst, Orchestrator, TradeExecutor) with `await asyncio.sleep(self.run_interval)` so the scheduler respects the per-agent cadence even when no work is happening.
2. For TradeExecutor, ensure `_poll_pending_orders()` still runs every `STATUS_POLL_INTERVAL` but the idle pause comes from `self.run_interval` instead of a fixed value; leave other backoff sleeps untouched.
3. Document or log the interval in each agent so operators can trace it if misconfigured (e.g., `logger.debug` in `run()` prior to sleeping).
  </action>
  <verify>
python - <<'PY'
import inspect
from agents.market_analyst import MarketAnalystAgent
from agents.orchestrator import OrchestratorAgent
from agents.trade_executor import TradeExecutorAgent

for cls in (MarketAnalystAgent, OrchestratorAgent, TradeExecutorAgent):
    source = inspect.getsource(cls.run)
    assert 'await asyncio.sleep(self.run_interval)' in source
print('All agent run() implementations sleep on run_interval')
PY
  </verify>
  <done>All run() implementations now sleep using the configured run_interval so MarketAnalyst, Orchestrator, and TradeExecutor each observe the sub-minute cadence defined in settings.</done>
</task>

<task type="auto">
  <name>Task 3: Emit pipeline events whenever insights, signals, or executions happen</name>
  <files>
backend/agents/manager.py
backend/agents/market_analyst.py
backend/agents/orchestrator.py
backend/agents/trade_executor.py
  </files>
  <action>
1. Expose a `record_pipeline_event` helper on `AgentManager` (already defined) and ensure the wiring from Task 1 gives every agent access to `agent_manager.record_pipeline_event` plus any metadata helpers (`event_type`, `channel`, `priority`).
2. In `MarketAnalystAgent._publish_insight`, call `self.agent_manager.record_pipeline_event` after `publish_reliable` succeeds, tagging the event as `source_agent='market_analyst'`, `target_agent='orchestrator'`, `event_type='insight'`, `channel=Channels.STREAM_TRADE_SIGNALS`, and include the `summary`/`analysis_bundle` for context.
3. In `OrchestratorAgent._maybe_create_trade_signal` right after a signal is published to Redis Streams, emit a pipeline event for the signal with `target_agent='trade_executor'` and priority-level metadata.
4. In `TradeExecutorAgent._handle_signal` (and any retry/fallback paths), emit pipeline events when a signal is consumed, when fallback reduces volume, and when retries happen so the timeline always reflects progress; include `target_agent='kraken'` or `target_agent='trade_executor'` as appropriate.
5. If necessary, reset throughput counters (call `reset_throughput_counters`) every time `get_queue_metrics` is called so throughput-per-minute keeps tracking only recent events.
  </action>
  <verify>
python - <<'PY'
import inspect
from agents.market_analyst import MarketAnalystAgent
from agents.orchestrator import OrchestratorAgent
from agents.trade_executor import TradeExecutorAgent

for cls in (MarketAnalystAgent, OrchestratorAgent, TradeExecutorAgent):
    source = inspect.getsource(cls)
    assert 'record_pipeline_event' in source
print('Pipeline events are recorded from every agent')
PY
  </verify>
  <done>MarketAnalyst, Orchestrator, and TradeExecutor each invoke `record_pipeline_event` when insights flow, signals dispatch, or executions/retries occur so the pipeline timeline and throughput metrics feed real data.</done>
</task>

</tasks>

<verification>
Re-run Phase 02 verification (e.g., rerun `/gsd-verify-phase 02` or the suite that produced `02-VERIFICATION`) to confirm the two gaps are now satisfied. Expect the report to mark truths #5 and #6 as verified.
</verification>

<success_criteria>
1. Phase 02 verification marks the configurable agent cadence and pipeline timeline truths as ✅.
2. `/api/agents/dashboard` returns non-empty `pipeline_events` and non-zero `throughput_per_minute` when there are recent insights/signals.
3. Agent logs mention their configured intervals and pipeline events capture priority + source metadata for audit trails.
</success_criteria>

<output>
After completion, create `.planning/phases/02-autonomous-agent-loop/02-10-SUMMARY.md` describing how the verification gaps were addressed.
</output>
