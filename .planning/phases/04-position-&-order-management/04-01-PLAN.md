---
phase: 04-position-&-order-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/api/trades.py, backend/core/risk.py, backend/core/paper_trading.py, backend/tests/api/test_trades_order_entry.py]
autonomous: true
must_haves:
  truths:
    - "User can submit manual long/short market and limit orders from the API contract used by the Live Trading UI."
    - "Every manual open/close order request is gated by RiskService before execution is recorded."
    - "Order sizing can be derived from risk settings and account balance instead of requiring fixed quantity only."
  artifacts:
    - path: "backend/api/trades.py"
      provides: "Manual order entry/close endpoints with market+limit support and risk-aware sizing"
    - path: "backend/core/risk.py"
      provides: "Reusable account-equity/sizing helper used by manual order endpoints"
    - path: "backend/tests/api/test_trades_order_entry.py"
      provides: "Regression coverage for market+limit entry and risk gate behavior"
  key_links:
    - from: "backend/api/trades.py"
      to: "backend/core/risk.py"
      via: "RiskService validation and size calculation before persisting orders"
    - from: "backend/api/trades.py"
      to: "backend/core/paper_trading.py"
      via: "Market-order path executes via paper engine to produce open position state"
---

<objective>
Introduce a lifecycle-safe manual order entry backend that supports market and limit opens, partial-close intent, and risk-based sizing.

Purpose: Satisfy POS-01/POS-02/POS-03/POS-04 with server-enforced rules before any UI wiring.
Output: New/updated trades API contracts plus backend tests proving risk-gated market/limit behavior.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-position-&-order-management/04-CONTEXT.md
@.planning/phases/04-position-&-order-management/04-RESEARCH.md
@.planning/phases/03-core-risk-management/03-01-SUMMARY.md
@.planning/phases/03-core-risk-management/03-03-SUMMARY.md
@backend/api/trades.py
@backend/core/risk.py
@backend/core/paper_trading.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add risk-gated manual order submit endpoint for market and limit</name>
  <files>backend/api/trades.py, backend/core/risk.py</files>
  <action>
    Add a dedicated endpoint contract (keep existing `/api/trades/` backward-compatible) for manual entry with explicit order intent:
    - Request supports `symbol`, `side`, `order_type` (`market|limit`), `limit_price` (required for limit), and either `quantity` OR `risk_percent` (1-100).
    - Implement server-side sizing helper in `RiskService` (or a colocated helper) that derives quantity from current account equity snapshot + reference price when `risk_percent` is used.
    - Validate with `RiskService.validate_trade(...)` before creating any `Trade`/`Order` rows.
    - For `market`: execute via paper trading engine path so open positions become immediately visible.
    - For `limit`: create pending order record with normalized status payload (`pending`, `partially_filled`, `filled`, `rejected`, `canceled`) and no fake immediate fill.
    - Return response fields needed by UI review/outcome panels: `order_id`, `status`, `reason_code`, `reason_message`, `requested_quantity`, `filled_quantity`.
    Do not bypass RiskService or perform client-only risk calculations.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/api/test_trades_order_entry.py -k "market or limit or risk" -q`.
  </verify>
  <done>Manual order submit API accepts market and limit payloads, enforces risk gate, and returns lifecycle-ready order status metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add close-position API support for partial quantity and market execution semantics</name>
  <files>backend/api/trades.py, backend/core/paper_trading.py</files>
  <action>
    Upgrade close flow to match locked UX decisions:
    - Extend close request model to accept `quantity` for partial closes; default remains full close when omitted.
    - Execute close as market-priced action using latest ticker/engine price (not user-entered exit price).
    - Persist close intent details (`requested_quantity`, `executed_price`, `close_reason`) and return normalized outcome object with short `reason_code` values for failures.
    - Ensure close failures return structured errors that UI can map to toast + retry prompt.
    Keep stop-loss behavior from Phase 03 intact and do not remove existing close endpoint route.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/api/test_trades_order_entry.py -k "close or partial" -q`.
  </verify>
  <done>Close endpoint supports full and partial close requests with market execution semantics and structured failure reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused API regression coverage for entry and close contracts</name>
  <files>backend/tests/api/test_trades_order_entry.py</files>
  <action>
    Create/extend test coverage for Phase 04 entry primitives:
    - Market buy/sell submission passes risk gate and returns lifecycle fields.
    - Limit order submission requires `limit_price`, is stored as pending, and does not create filled position immediately.
    - `risk_percent` sizing computes deterministic quantity from mocked equity + price.
    - Risk gate rejection returns typed payload and no order/trade persistence.
    - Partial close reduces remaining quantity; full close marks position exited.
    Mock Kraken/ticker dependencies for deterministic tests.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/api/test_trades_order_entry.py -q`.
  </verify>
  <done>Backend contract behavior for market/limit entry, risk sizing, and partial/full close is locked by automated tests.</done>
</task>

</tasks>

<verification>
1. `POST /api/trades/orders` accepts both market and limit payloads and always executes RiskService checks first.
2. Limit orders remain pending after submission until lifecycle reconciliation marks them terminal.
3. Close endpoint supports partial close quantity and returns structured result metadata.
</verification>

<success_criteria>
POS-01 through POS-04 are backend-ready with stable API contracts and regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-position-&-order-management/04-01-SUMMARY.md`
</output>
