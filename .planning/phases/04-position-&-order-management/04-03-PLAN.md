---
phase: 04-position-&-order-management
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified: [frontend/src/pages/LiveTrading.js, frontend/src/components/PositionManager.js, frontend/src/components/OrderTicket.js, frontend/src/components/OrderOutcomeFeed.js, frontend/src/services/api.js]
autonomous: false
must_haves:
  truths:
    - "User can submit long/short market or limit orders from the Live Trading ticket, always via review confirmation."
    - "Active positions and pending orders are shown in separate UI sections with dollar-first real-time P&L emphasis."
    - "User can close full or partial positions from inline controls with confirmation and clear retry messaging on failure."
    - "Order outcomes appear in both ticket-local status and a global feed with rejection reason text + short code."
  artifacts:
    - path: "frontend/src/components/OrderTicket.js"
      provides: "Ticket flow with market/limit toggle, remembered side, prefilled limit, review modal"
    - path: "frontend/src/components/PositionManager.js"
      provides: "Desktop table + mobile-priority position view with inline partial/full close"
    - path: "frontend/src/components/OrderOutcomeFeed.js"
      provides: "Global order activity feed for pending, success, and rejection outcomes"
  key_links:
    - from: "frontend/src/components/OrderTicket.js"
      to: "frontend/src/services/api.js"
      via: "submit/review flow calls manual order endpoint"
    - from: "frontend/src/services/api.js"
      to: "backend/api/trades.py"
      via: "manual order + position/pending lifecycle endpoints return reason_code/reason_message outcome payloads"
    - from: "frontend/src/components/PositionManager.js"
      to: "backend/api/trades.py"
      via: "active positions + pending orders + partial/full close endpoints"
    - from: "frontend/src/pages/LiveTrading.js"
      to: "frontend/src/components/OrderOutcomeFeed.js"
      via: "normalized lifecycle outcomes from api.js are appended to global feed entries"
---

<objective>
Implement the locked Phase 04 UX for order entry, positions, pending orders, and close flows on desktop and mobile.

Purpose: Deliver POS-06 and complete user-facing acceptance for Phase 04 lifecycle behavior.
Output: Updated Live Trading page and components with confirmation-first flows and outcome visibility.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-position-&-order-management/04-CONTEXT.md
@.planning/phases/04-position-&-order-management/04-RESEARCH.md
@.planning/phases/02-autonomous-agent-loop/02-09-SUMMARY.md
@frontend/src/pages/LiveTrading.js
@frontend/src/components/PositionManager.js
@frontend/src/services/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build confirmation-first order ticket with market/limit and remembered side</name>
  <files>frontend/src/components/OrderTicket.js, frontend/src/services/api.js, frontend/src/pages/LiveTrading.js</files>
  <action>
    Implement ticket UX decisions exactly:
    - Replace inline trade form in `LiveTrading.js` with `OrderTicket` component.
    - Support market/limit toggle, buy/sell side, quantity/risk-percent inputs, and limit prefill from current market price.
    - Persist last selected side in localStorage and hydrate as smart default on next render.
    - Require review modal confirmation before submit; API call only occurs on confirm.
    - Show ticket-local status panel for `pending`, `partially_filled`, `filled`, `rejected` with reason code/message when present.
    Keep styling aligned with existing Live Trading dark-theme visual language.
  </action>
  <verify>
    Run `npm --prefix frontend run lint`.
  </verify>
  <done>Order ticket supports market/limit with review confirmation, remembers side, and surfaces localized submission outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor positions UI into table-first positions + separate pending section with close confirmations</name>
  <files>frontend/src/components/PositionManager.js, frontend/src/services/api.js</files>
  <action>
    Align positions and close UX with locked decisions:
    - Render desktop table with expandable details and mobile-priority compact rows (symbol, side, dollar P&L first).
    - Keep open positions and pending orders in separate sections; do not merge datasets.
    - Use green/red P&L coloring with dollar value primary and percent secondary.
    - Add inline Close action supporting partial quantity quick entry and mandatory confirmation dialog.
    - On close failure, display toast-style feedback and explicit retry action.
    - Trigger temporary row highlight for newly filled or recently closed positions.
  </action>
  <verify>
    Run `npm --prefix frontend run lint`.
  </verify>
  <done>Live Trading shows clear open-vs-pending separation, responsive position rendering, and confirmation-based partial/full close flows.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and wire global OrderOutcomeFeed from lifecycle outcome data</name>
  <files>frontend/src/pages/LiveTrading.js, frontend/src/components/OrderOutcomeFeed.js, frontend/src/services/api.js</files>
  <action>
    Add explicit global outcome feed wiring so the locked "local + global outcomes" decision is implemented in code:
    - Define/extend normalized outcome mapping in `api.js` for manual order submit and lifecycle responses from `backend/api/trades.py` endpoints, preserving `status`, `reason_code`, and human-readable `reason_message`.
    - In `LiveTrading.js`, maintain a bounded order outcome list (newest first) that receives events from ticket submit outcomes and follow-up lifecycle updates (including rejected/failed close attempts).
    - Render `OrderOutcomeFeed` on Live Trading and pass normalized entries with timestamp, symbol, side, status, short code, and reason text.
    - Keep ticket-local status logic in `OrderTicket` unchanged except for emitting/forwarding normalized outcomes to the page-level feed state.
  </action>
  <verify>
    Run `npm --prefix frontend run lint`.
  </verify>
  <done>Global `OrderOutcomeFeed` is populated from real lifecycle API outcomes and shows reason text + short code alongside ticket-local outcome states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 04 UI flow: ticket review modal, pending/positions split, inline partial close, and wired local+global outcome surfacing</what-built>
  <how-to-verify>
    1. Start app with `./init.sh` and log in using configured `.env` AI credentials.
    2. Open `/live-trading` and submit one market order and one limit order from the new ticket; confirm each through the review modal.
    3. Confirm pending limit order appears only in Pending Orders section, not Active Positions.
    4. Confirm active position rows show dollar-first P&L with green/red convention and expandable details on desktop.
    5. Use inline Close to partially close a position, confirm modal, and verify remaining quantity updates.
    6. Trigger one failure case (invalid close quantity or forced backend rejection) and confirm toast + retry prompt shows.
    7. Verify order outcomes appear in both ticket-local status and global order outcome feed with reason code/message for rejection.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or provide exact UI/behavior mismatches to fix.</resume-signal>
</task>

</tasks>

<verification>
1. Live Trading page renders new order ticket and updated positions/pending sections on desktop and mobile widths.
2. Confirmation modals gate submit and close actions.
3. Outcome states and rejection metadata are visible in both local and global UI surfaces.
</verification>

<success_criteria>
Dashboard/Live Trading experience satisfies Phase 04 UX constraints: real-time position visibility, manual open/close, pending lifecycle visibility, and actionable failure outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-position-&-order-management/04-03-SUMMARY.md`
</output>
