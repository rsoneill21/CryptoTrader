---
phase: 04-position-&-order-management
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [backend/api/trades.py, backend/services/trade_sync.py, backend/tests/services/test_order_lifecycle_sync.py, backend/tests/api/test_trades_order_lifecycle.py]
autonomous: true
must_haves:
  truths:
    - "Pending orders transition through pending/partially_filled/filled/rejected/canceled states without drifting from source data."
    - "Partial fills are persisted and reflected as partial exposure until fully filled or closed."
    - "Order failures surface actionable reason message + short code for both ticket and global feeds."
  artifacts:
    - path: "backend/services/trade_sync.py"
      provides: "Order lifecycle reconciliation logic for pending and partial-fill states"
    - path: "backend/api/trades.py"
      provides: "Pending-order listing + status refresh endpoints backed by reconciliation service"
    - path: "backend/tests/services/test_order_lifecycle_sync.py"
      provides: "Lifecycle transition tests (pending -> partial -> terminal)"
  key_links:
    - from: "backend/api/trades.py"
      to: "backend/services/trade_sync.py"
      via: "pending/status endpoints call reconciliation before returning UI data"
    - from: "backend/services/trade_sync.py"
      to: "db.models.Order"
      via: "filled quantity and terminal status persistence"
---

<objective>
Implement a single reconciliation path for pending and partial order lifecycle so UI and backend remain consistent.

Purpose: Deliver POS-05 and POS-07 while protecting state integrity for limit and partially filled orders.
Output: Reconciliation service, lifecycle-aware trade endpoints, and regression tests.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-position-&-order-management/04-CONTEXT.md
@.planning/phases/04-position-&-order-management/04-RESEARCH.md
@.planning/phases/03-core-risk-management/03-02-SUMMARY.md
@backend/services/trade_sync.py
@backend/api/trades.py
@backend/services/kraken.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend trade sync service with explicit order lifecycle reconciliation</name>
  <files>backend/services/trade_sync.py</files>
  <action>
    Add lifecycle reconciliation helpers that operate on local `Order` rows with exchange IDs:
    - Normalize statuses into `pending`, `partially_filled`, `filled`, `rejected`, `canceled` (map Kraken values deterministically).
    - Persist `filled_quantity` updates incrementally; mark partial state when `0 < filled_quantity < quantity`.
    - Promote to terminal state only when filled quantity reaches requested quantity OR exchange returns terminal status.
    - Capture machine-friendly `reason_code` + human `reason_message` on rejection/failure paths.
    - Keep logic idempotent so repeated refresh calls do not duplicate side effects.
    Do not mix pending orders into active position query logic.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/services/test_order_lifecycle_sync.py -q`.
  </verify>
  <done>Reconciliation service can repeatedly sync pending orders while preserving accurate partial and terminal lifecycle state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lifecycle refresh into trades API pending and status endpoints</name>
  <files>backend/api/trades.py</files>
  <action>
    Update API endpoints so clients always get reconciled lifecycle state:
    - Add/extend `GET /api/trades/orders/pending` to return only non-terminal orders with lifecycle metadata and related trade context.
    - Update order status endpoint to call reconciliation helper before serialization.
    - Ensure rejected/failed responses include both `reason_code` and `reason_message` fields used by UI ticket and activity feed.
    - Add retry-safe error handling for exchange outages (typed service-unavailable response, no silent pass).
    Keep AsyncSession patterns and existing auth dependencies.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/api/test_trades_order_lifecycle.py -k "pending or status or rejection" -q`.
  </verify>
  <done>Pending/status endpoints return lifecycle-consistent data including partial fills and actionable rejection metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for partial fills, terminal transitions, and failure metadata</name>
  <files>backend/tests/services/test_order_lifecycle_sync.py, backend/tests/api/test_trades_order_lifecycle.py</files>
  <action>
    Add tests covering:
    - Pending -> partially_filled -> filled progression across multiple sync calls.
    - Pending -> rejected/canceled transition with reason code/message retained.
    - API pending list excludes filled/canceled orders and includes partials.
    - API status refresh is idempotent and does not regress filled quantity.
    - Exchange failure path returns structured error and leaves prior persisted state intact.
  </action>
  <verify>
    Run `backend/venv/bin/python -m pytest backend/tests/services/test_order_lifecycle_sync.py backend/tests/api/test_trades_order_lifecycle.py -q`.
  </verify>
  <done>Lifecycle reconciliation behavior is locked with deterministic backend tests for partial fill and failure cases.</done>
</task>

</tasks>

<verification>
1. Pending orders endpoint returns only non-terminal rows and includes partial fill quantities.
2. Reconciliation updates status and filled quantity consistently across repeated refresh calls.
3. Rejections expose both human and machine-readable reason metadata for downstream UI.
</verification>

<success_criteria>
POS-05 and POS-07 are satisfied with explicit order lifecycle state management and tested reconciliation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-position-&-order-management/04-02-SUMMARY.md`
</output>
