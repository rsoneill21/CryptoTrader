---
phase: 03-core-risk-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [backend/core/rate_limit.py, backend/core/risk.py, backend/services/kraken.py, backend/agents/trade_executor.py]
autonomous: true
must_haves:
  truths:
    - "Trade execution is blocked before order placement when liquidity checks fail."
    - "Kraken-bound requests are throttled before exchange rejection occurs."
    - "Live signal-to-order flow calls risk validation before any order placement attempt."
  artifacts:
    - path: "backend/core/rate_limit.py"
      contains: "class KrakenRateLimiter"
  key_links:
    - from: "backend/services/kraken.py"
      to: "backend/core/rate_limit.py"
      via: "await KrakenRateLimiter.acquire()"
    - from: "backend/agents/trade_executor.py"
      to: "backend/core/risk.py"
      via: "_handle_signal calls RiskService.validate_trade before _place_order_with_retries"
---

<objective>
Implement Kraken-specific rate limiting and liquidity-based trade validation.

Purpose: Protect the account from API bans (RISK-07) and prevent execution in illiquid markets (RISK-06).
Output: Redis-based async rate limiter and liquidity check integration.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-risk-management/03-01-SUMMARY.md
@backend/core/rate_limit.py
@backend/services/kraken.py
@backend/agents/trade_executor.py
</context>

<tasks>

<task type="auto">
  <name>Implement Async Kraken Call Counter (Redis)</name>
  <files>backend/core/rate_limit.py</files>
  <action>
    Implement `KrakenRateLimiter` class:
    - Uses Redis to maintain a counter that increments per API call.
    - Implements the "decay" logic (e.g., -0.5 per second or as per Kraken tier).
    - Async method `acquire(weight)`: 
      - Fetches `kraken_tier` from settings.
      - If counter + weight > limit, `await asyncio.sleep()` until enough decay has happened, or raise exception if timeout exceeded.
      - Ensure it is non-blocking to the event loop.
  </action>
  <verify>
    - Add tests in `backend/tests/core/test_kraken_rate_limiter.py` for decay, concurrency, and wait-on-limit behavior.
    - Run `cd backend && pytest tests/core/test_kraken_rate_limiter.py -q`.
  </verify>
  <done>Kraken-specific rate limiting is available via an async Redis-based counter.</done>
</task>

<task type="auto">
  <name>Integrate Rate Limiter into KrakenService</name>
  <files>backend/services/kraken.py</files>
  <action>
    Modify `KrakenService._request` (or equivalent central request method) to `await KrakenRateLimiter.acquire()` before making the actual HTTP call.
    Assign weights based on endpoint (default 1, some endpoints might be more).
  </action>
  <verify>
    - Add service-level tests in `backend/tests/services/test_kraken_rate_limit_integration.py` and assert `_request` acquires limiter before sending HTTP requests.
    - Run `cd backend && pytest tests/services/test_kraken_rate_limit_integration.py -q`.
  </verify>
  <done>KrakenService is protected by the async rate limiter.</done>
</task>

<task type="auto">
  <name>Implement Liquidity Verification and Execution-Path Risk Gate</name>
  <files>backend/core/risk.py, backend/agents/trade_executor.py</files>
  <action>
    Add `check_liquidity(symbol, quantity, side)` to `RiskService`:
    - Fetches order book from `MarketAnalyst` or directly from `KrakenService`.
    - Calculates slippage/depth: ensure `quantity` can be filled within `min_liquidity_threshold` % of top price or minimum volume exists.
    - Update `validate_trade` to call `check_liquidity`.
    Integrate `RiskService.validate_trade(...)` into `TradeExecutorAgent._handle_signal` before any order placement (including fallback path), and reject/log signals that fail risk validation.
  </action>
  <verify>
    - Add risk tests in `backend/tests/core/test_risk_liquidity.py` with thin order book fixtures.
    - Run `cd backend && pytest tests/core/test_risk_liquidity.py -q`.
    - Add `backend/tests/agents/test_trade_executor_risk_gate.py` to assert violating signals never reach order placement.
    - Run `cd backend && pytest tests/agents/test_trade_executor_risk_gate.py -q`.
  </verify>
  <done>Trades are rejected for poor liquidity, and every live signal is risk-validated before any order placement call.</done>
</task>

</tasks>

<verification>
1. Hammer a Kraken endpoint in a loop and verify the rate limiter prevents 429s from the exchange by delaying requests.
2. Verify `RiskService` rejects trades on symbols with no order book data or very low volume.
</verification>

<success_criteria>
The system prevents Kraken rate limit violations using an async limiter and rejects trades that would suffer excessive slippage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-risk-management/03-02-SUMMARY.md`
</output>
