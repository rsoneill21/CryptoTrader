---
phase: 03-core-risk-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [backend/core/rate_limit.py, backend/core/risk.py, backend/services/kraken.py]
autonomous: true
must_haves:
  truths:
    - "System tracks Kraken call counter in Redis with appropriate decay"
    - "Trades are rejected if order book depth is insufficient for order size"
    - "Kraken API calls respect the rate limit counter before execution"
  artifacts:
    - path: "backend/core/rate_limit.py"
      contains: "class KrakenRateLimiter"
  key_links:
    - from: "backend/services/kraken.py"
      to: "backend/core/rate_limit.py"
      via: "KrakenRateLimiter.acquire()"
---

<objective>
Implement Kraken-specific rate limiting and liquidity-based trade validation.

Purpose: Protect the account from API bans (RISK-07) and prevent execution in illiquid markets (RISK-06).
Output: Redis-based rate limiter and liquidity check integration.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-risk-management/03-01-SUMMARY.md
@backend/core/rate_limit.py
@backend/services/kraken.py
</context>

<tasks>

<task type="auto">
  <name>Implement Kraken Call Counter (Redis)</name>
  <files>backend/core/rate_limit.py</files>
  <action>
    Implement `KrakenRateLimiter` class:
    - Uses Redis to maintain a counter that increments per API call.
    - Implements the "decay" logic (e.g., -0.5 per second or as per Kraken tier).
    - Method `acquire(weight)`: Blocks or raises exception if counter + weight > limit.
    - Ensure it handles "Tier" levels (Starter, Intermediate, Pro) via settings.
  </action>
  <verify>
    Test script that simulates rapid API calls and verifies the counter increases and then decays over time in Redis.
  </verify>
  <done>Kraken-specific rate limiting is available via Redis.</done>
</task>

<task type="auto">
  <name>Integrate Rate Limiter into KrakenService</name>
  <files>backend/services/kraken.py</files>
  <action>
    Modify `KrakenService._request` (or equivalent central request method) to use `KrakenRateLimiter.acquire()` before making the actual HTTP call.
    Assign weights based on endpoint (default 1, some endpoints might be more).
  </action>
  <verify>
    Perform multiple calls to KrakenService and verify they are throttled when the limit is reached.
  </verify>
  <done>KrakenService is protected by the rate limiter.</done>
</task>

<task type="auto">
  <name>Implement Liquidity Verification</name>
  <files>backend/core/risk.py</files>
  <action>
    Add `check_liquidity(symbol, quantity, side)` to `RiskService`:
    - Fetches order book from `MarketAnalyst` or directly from `KrakenService`.
    - Calculates slippage/depth: ensure `quantity` can be filled within `min_liquidity_threshold` % of top price or minimum volume exists.
    - Update `validate_trade` to call `check_liquidity`.
  </action>
  <verify>
    Mock a thin order book and verify `RiskService` rejects a large trade signal.
  </verify>
  <done>Trades are gated by available market liquidity.</done>
</task>

</tasks>

<verification>
1. Hammer a Kraken endpoint in a loop and verify the rate limiter prevents 429s from the exchange.
2. Verify `RiskService` rejects trades on symbols with no order book data or very low volume.
</verification>

<success_criteria>
The system prevents Kraken rate limit violations and rejects trades that would suffer excessive slippage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-risk-management/03-02-SUMMARY.md`
</output>
