---
phase: 03-core-risk-management
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [backend/core/risk.py, backend/core/paper_trading.py, backend/core/trading_control.py]
autonomous: true
must_haves:
  truths:
    - "Trading is automatically paused when daily P&L hits the loss limit"
    - "Paper trading positions are closed automatically when stop-loss price is reached"
    - "Manual resume is required after a daily loss halt"
  artifacts:
    - path: "backend/core/paper_trading.py"
      contains: "stop_loss"
  key_links:
    - from: "backend/core/risk.py"
      to: "backend/core/trading_control.py"
      via: "trading_control.pause_trading()"
---

<objective>
Implement active protection measures: daily loss limits and engine-side stop-losses.

Purpose: Minimize losses from individual trades (RISK-02) and prevent catastrophic "tilt" or market crashes (RISK-03).
Output: Daily halt logic and Paper Trading stop-loss enforcement.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-risk-management/03-01-SUMMARY.md
@backend/core/paper_trading.py
@backend/core/trading_control.py
</context>

<tasks>

<task type="auto">
  <name>Implement Daily Loss Tracking and Halt</name>
  <files>backend/core/risk.py, backend/core/trading_control.py</files>
  <action>
    1. Add `check_daily_halt(db)` to `RiskService`:
       - Query `Trade` table for today's realized P&L.
       - Compare vs `daily_loss_limit`.
       - If cumulative P&L <= -daily_loss_limit, call `trading_control.pause_trading(reason="Daily loss limit reached")`.
    2. Add a background task (or call from `validate_trade`) to trigger this check.
  </action>
  <verify>
    Create trades totaling -$501 when limit is $500, and verify `trading_control.is_paused()` becomes True.
  </verify>
  <done>System automatically halts on daily loss breach.</done>
</task>

<task type="auto">
  <name>Add Stop-Loss to Paper Trading Engine</name>
  <files>backend/core/paper_trading.py</files>
  <action>
    1. Update `PaperPosition` and `_MutablePosition` to include `stop_loss_price: Optional[float]`.
    2. Update `PaperTradeSignal` to accept an optional `stop_loss`.
    3. Update `PaperTradingEngine._open_position` to set the stop-loss price.
    4. Update `PaperTradingEngine.update_market_price`:
       - After updating price, iterate through open positions for that symbol.
       - If `side == BUY` and `price <= stop_loss_price`, trigger `_close_positions`.
       - If `side == SELL` and `price >= stop_loss_price`, trigger `_close_positions`.
       - Record "Stop-Loss Triggered" in exit reasoning.
  </action>
  <verify>
    - Open a paper position at $100 with $95 SL.
    - Update price to $94.
    - Verify position is automatically closed with $94 exit price.
  </verify>
  <done>Paper trading engine enforces stop-losses in real-time.</done>
</task>

<task type="auto">
  <name>Integrated Risk Flow Verification</name>
  <files>backend/core/risk.py</files>
  <action>
    Perform an integration test:
    - Try to open a trade that exceeds `max_position_size_pct` -> Verify rejection.
    - Open a valid trade -> Verify success.
    - Reach `max_concurrent_positions` -> Verify next trade is rejected.
    - Trigger a stop-loss -> Verify position closes.
    - Total losses hit daily limit -> Verify global trading halt.
  </action>
  <verify>
    Logs show each risk rejection reason correctly.
  </verify>
  <done>Full risk management lifecycle verified.</done>
</task>

</tasks>

<verification>
1. Verify `trading_control` state is persisted/respected.
2. Verify `PaperPosition` pydantic model includes `stop_loss_price`.
3. Check `SystemLog` for "Daily loss limit reached" entries.
</verification>

<success_criteria>
The system proactively closes losing positions and stops all trading if the daily loss threshold is crossed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-risk-management/03-03-SUMMARY.md`
</output>
