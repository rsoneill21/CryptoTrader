---
phase: 07-ai-chat-integration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/ai.py
  - frontend/src/components/ChatWindow.js
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "AI responds with knowledge of portfolio/risk context"
    - "Chat history loads and displays past messages"
    - "Historical messages retain rich UI elements (badges, impact)"
  artifacts:
    - path: "backend/api/ai.py"
      provides: "Context grounding and rich persistence"
    - path: "frontend/src/components/ChatWindow.js"
      provides: "History rendering logic"
  key_links:
    - from: "backend/api/ai.py"
      to: "db.chat_history.ai_response"
      via: "json.dumps(contract)"
      pattern: "json\.dumps.*contract"
---

<objective>
Close verification gaps in AI chat integration: ensure context is grounded, history renders correctly, and rich metadata is persisted.

Purpose: Fix identified regressions where AI lacked context, history UI was blank, and rich elements were lost on reload.
Output: Patched backend API and frontend component.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/api/ai.py
@frontend/src/components/ChatWindow.js
@backend/services/chat_context.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Backend Grounding and Persistence</name>
  <files>backend/api/ai.py</files>
  <action>
    Modify `_streaming_chat_response` in `backend/api/ai.py`:
    1. Initialize `contract = None` before the `try` block.
    2. Capture the normalized `contract` inside the `try` block (when `normalizer.normalize` is called).
    3. In the `finally` block:
       - Check if `contract` exists.
       - If yes, use `contract` to populate `final_payload` (specifically `recommendations`, `portfolio_impact`, `summary_paragraph`).
       - If no, fall back to existing extraction logic (`policy` + `trade_explanation` extraction).
    4. Ensure `request.context_json` is explicitly assigned and maybe log its keys at `INFO` level to prove grounding.
    
    This fixes "AI Not Grounded" (by verifying/logging) and "History Richness Lost" (by persisting the actual contract).
  </action>
  <verify>
    Check that `final_payload` construction in `finally` uses `contract` variable.
    Check that `request.context_json` is assigned before stream starts.
  </verify>
  <done>
    Rich metadata (badges/impact) is saved to DB via the normalized contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Frontend History Rendering</name>
  <files>frontend/src/components/ChatWindow.js</files>
  <action>
    Modify `normalizeHistoryMessages` in `frontend/src/components/ChatWindow.js`:
    1. Ensure input `history` array is safe (default to empty array if null).
    2. Explicitly assign sorted results to a variable `result`.
    3. Return `result` at the end of the function.
    
    This fixes "Chat History Display Blocked" by removing any ambiguity about the return statement.
  </action>
  <verify>
    Check for `return result;` or `return sortedRows;` at end of function.
  </verify>
  <done>
    History normalization function returns array reliably.
  </done>
</task>

</tasks>

<verification>
Manual verification steps (post-execution):
1. Reload page -> History should appear.
2. Send message asking for trade rationale -> Response should have badges.
3. Reload page again -> Last message should STILL have badges (persistence check).
</verification>

<success_criteria>
- AI context is explicitly logged/passed.
- Chat history rendering function has valid return.
- Persisted chat history includes "recommendations" and "portfolio_impact" from the normalized contract.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-08-SUMMARY.md`
</output>
