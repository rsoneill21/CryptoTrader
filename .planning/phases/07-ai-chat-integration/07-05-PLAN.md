---
phase: 07-ai-chat-integration
plan: 05
type: execute
wave: 2
depends_on: ["07-04"]
files_modified: ["frontend/src/services/api.js", "frontend/src/components/ChatWindow.js"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "UI renders Primary and Backup recommendations with distinct actions and rationales"
    - "UI renders Portfolio Impact as a visible metric or status line"
    - "UI renders structured Trade Rationale (Thesis, Signals, Risk, Counterfactual)"
  artifacts:
    - path: "frontend/src/components/ChatWindow.js"
      provides: "Rich rendering components for trading metadata"
    - path: "frontend/src/services/api.js"
      provides: "Updated normalization for trade_explanation"
  key_links:
    - from: "ChatWindow.js"
      to: "message.structuredResponse.tradeExplanation"
      via: "conditional rendering"
---

<objective>
Implement high-fidelity rendering for recommendations, portfolio impact, and structured trade rationales in the Chat UI.

Purpose: Fixes Gaps 1 (Recommendations), 2 (Why-trade Rendering), and 3 (Portfolio Impact).
Output: Complete AI Chat UI with rich trading-grounded visualizations.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ai-chat-integration/07-04-PLAN.md
@.planning/phases/07-ai-chat-integration/VERIFICATION.md
@frontend/src/services/api.js
@frontend/src/components/ChatWindow.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend normalization for rich metadata</name>
  <files>frontend/src/services/api.js</files>
  <action>
    Update `normalizeAIChatPayload` in `frontend/src/services/api.js` to:
    1. Capture `trade_explanation` (mapped to `tradeExplanation`) and `portfolio_impact` from the payload.
    2. Update `hasStructuredContent` to return true if `recommendations`, `tradeExplanation`, or `portfolio_impact` are present (in addition to existing checks).
    3. Ensure `trade_explanation` fields (thesis, market_signals, etc.) are correctly preserved.
  </action>
  <verify>Check `normalizeAIChatPayload` with a mock object containing all rich fields and ensure the returned object includes them.</verify>
  <done>Frontend normalizer supports all backend metadata fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement UI components for Recommendations and Impact</name>
  <files>frontend/src/components/ChatWindow.js</files>
  <action>
    Add sub-components or render functions in `ChatWindow.js`:
    - `RecommendationDisplay`: Renders "Primary" and "Backup" badges with their respective actions (buy/sell/hold/reduce) and rationales. Use distinct colors (e.g., green for buy, amber for hold/reduce, gray for observe).
    - `ImpactDisplay`: Renders a "Portfolio Impact" section showing the impact string/percentage with a distinct icon or styling.
  </action>
  <verify>Manually trigger a mock message with `recommendations` and verify the badges and impact are visible.</verify>
  <done>Recommendations and Impact are visually represented in the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Why-Trade Rationale UI and Integration</name>
  <files>frontend/src/components/ChatWindow.js</files>
  <action>
    1. Create `RationaleDisplay` component to render the structured trade explanation:
       - Thesis: Italicized or bolded summary.
       - Market Signals: Bulleted list under a "Signals" header.
       - Risk Checks: Bulleted list under a "Risk Analysis" header.
       - Counterfactual: Small text section under a "What to watch for" header.
    2. Update `renderMessage` to integrate `RecommendationDisplay`, `ImpactDisplay`, and `RationaleDisplay` into the chat bubble when `message.structuredResponse` is present.
    3. Ensure clean spacing and high-contrast typography for readability.
  </action>
  <verify>Manually trigger a 'why trade' mock response and verify the full structured rationale renders correctly in the chat window.</verify>
  <done>Full rich chat responses are rendered correctly in the UI, closing all remaining gaps.</done>
</task>

</tasks>

<success_criteria>
- Recommendations are visible and color-coded.
- Portfolio impact is prominently displayed.
- Why-trade explanations are structured and readable.
- No raw JSON blocks appear in the chat text.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-05-SUMMARY.md`
</output>
