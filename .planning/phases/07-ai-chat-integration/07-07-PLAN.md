---
phase: 07-ai-chat-integration
plan: 07
type: execute
wave: 2
depends_on: [07-06]
files_modified:
  - backend/api/ai.py
autonomous: true
must_haves:
  truths:
    - "Chat history preserves recommendations, impact analysis, and rationales"
    - "Reloading the page shows the same rich UI (badges, risk checks) as the live stream"
  artifacts:
    - path: "backend/api/ai.py"
      contains: "json.dumps(final_payload)"
  key_links:
    - from: "backend/api/ai.py"
      to: "database.chat_history"
      via: "_persist_chat_history call"
---

<objective>
Persist rich chat metadata (recommendations, portfolio impact, trade rationales) in the database so that historical chats render with the same fidelity as live streams.

Purpose: Prevent "amnesia" where refreshing the page downgrades the chat experience to plain text.
Output: Updated backend persistence logic to store full structured JSON responses.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ai-chat-integration/07-06-SUMMARY.md
@backend/api/ai.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Structured Chat Persistence</name>
  <files>backend/api/ai.py</files>
  <action>
    Update `_streaming_chat_response` in `backend/api/ai.py` to persist the full structured response JSON instead of just plain text.
    
    1. Capture `policy` recommendations and `portfolio_impact`.
    2. Ensure `trade_explanation` (extracted from rationale tags) is accessible in the final persistence block.
    3. Construct a `final_payload` dictionary that matches what the frontend `normalizeAIChatPayload` expects:
       ```json
       {
         "text": "...",
         "summary_paragraph": "...",
         "recommendations": {...},
         "portfolio_impact": "...",
         "trade_explanation": {...},
         "provider": "...",
         "model": "..."
       }
       ```
    4. In the `finally` block, serialize this payload (`json.dumps(final_payload)`) and pass it as `ai_response` to `_persist_chat_history`.
    
    **Note:** The `ChatHistory.ai_response` column is text, so storing a JSON string is valid. The frontend already attempts to parse this field as JSON.
    
    **Implementation Detail:**
    - Initialize `accumulator` and `trade_explanation` outside the try/loop scope if needed to ensure availability in `finally`.
    - Handle cases where policy might be "refuse" or "clarify" (which already yield JSON contracts—ensure these are also persisted correctly).
  </action>
  <verify>
    Check `backend/api/ai.py`:
    - `_persist_chat_history` call in `finally` passes a JSON string (via `json.dumps`) instead of raw accumulator text.
    - The JSON object includes `recommendations` and `trade_explanation`.
  </verify>
  <done>
    Database stores rich metadata in the `ai_response` column, enabling high-fidelity history rendering.
  </done>
</task>

</tasks>

<verification>
No manual verification required. The frontend logic (`normalizeAIChatPayload`) is already built to handle JSON strings in this field.
</verification>

<success_criteria>
- Chat history entries contain structured JSON.
- Rich elements (badges, rationales) are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-07-SUMMARY.md`
</output>
