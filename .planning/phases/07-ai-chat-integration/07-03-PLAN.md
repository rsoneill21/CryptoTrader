---
phase: 07-ai-chat-integration
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified: [backend/api/ai.py, backend/tests/api/test_ai_chat_integration.py]
autonomous: true
must_haves:
  truths:
    - "Broad prompts (e.g., 'How am I doing?') return a clarifying follow-up before full analysis."
    - "Stale or incomplete trading context is refused with explicit refresh/missing-context guidance."
    - "Recommendation responses include a top recommendation and one backup, and elevated-risk mode defaults to hold/no-action."
    - "Why-trade responses include thesis, market signals, risk checks, and counterfactual trigger conditions."
    - "Confidence appears only when user explicitly asks, and portfolio impact appears only for position-changing recommendations."
  artifacts:
    - path: "backend/api/ai.py"
      provides: "Chat endpoint orchestration that applies context assembly + policy + normalized contract before and during SSE streaming"
    - path: "backend/tests/api/test_ai_chat_integration.py"
      provides: "Regression tests for clarify/refuse/elevated-risk/why-trade/contract behavior"
  key_links:
    - from: "backend/api/ai.py"
      to: "backend/services/chat_context.py"
      via: "context build + freshness validation before provider call"
      pattern: "ChatContextAssembler"
    - from: "backend/api/ai.py"
      to: "backend/services/chat_policy.py"
      via: "policy mode enforcement (clarify/refuse/answer)"
      pattern: "ChatPolicyEngine"
    - from: "backend/api/ai.py"
      to: "backend/services/chat_response.py"
      via: "contract normalization + hybrid rendering"
      pattern: "normalize|render"
---

<objective>
Wire the AI chat endpoint to enforce policy-driven, context-grounded behavior and emit a stable response contract for streaming and persistence.

Purpose: Ensure Phase 7 user-visible outcomes are guaranteed server-side instead of relying on free-form model output.
Output: Updated `/api/ai/chat` orchestration with contract enforcement and full API regression coverage.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-chat-integration/07-CONTEXT.md
@.planning/phases/07-ai-chat-integration/07-RESEARCH.md
@.planning/phases/07-ai-chat-integration/07-01-SUMMARY.md
@backend/api/ai.py
@backend/core/exceptions.py
@backend/api/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce context + policy + contract in /api/ai/chat orchestration</name>
  <files>backend/api/ai.py</files>
  <action>Update chat request flow to: (1) assemble context via `ChatContextAssembler`, (2) evaluate `ChatPolicyEngine`, (3) enforce clarify/refuse branches before provider streaming, and (4) normalize/render responses with `chat_response` contract helpers. For refuse/clarify, emit deterministic SSE payloads and skip provider calls. For answer mode, stream chunks while preserving final normalized contract metadata (`mode`, `timeframe_used`, `guardrail`, recommendation structure, trade explanation structure). Keep exception handling aligned with typed API patterns (no new bare `HTTPException` catch-all behavior).</action>
  <verify>`cd backend && pytest tests/api/test_ai_chat_integration.py -k "clarify or stale_context or incomplete_context"`</verify>
  <done>Endpoint always gates by context/policy first, then streams only contract-compliant responses and persists normalized chat context metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add API regression tests for locked chat behaviors</name>
  <files>backend/tests/api/test_ai_chat_integration.py</files>
  <action>Create targeted async API tests covering roadmap + locked decisions: broad prompt clarify first; stale context refusal; incomplete context refusal; elevated-risk default hold/no-action with one backup; aggressive request conflict returns safer alternative + rationale; why-trade includes thesis/signals/risk/counterfactual; confidence included only when explicitly requested; portfolio impact included only for position-changing recommendations. Assert contract fields are present and correctly shaped.</action>
  <verify>`cd backend && pytest tests/api/test_ai_chat_integration.py`</verify>
  <done>Regression suite fails on any policy/contract drift and proves all required chat behaviors are enforced server-side.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/api/test_ai_chat_integration.py`
- `cd backend && pytest tests/services/test_chat_context.py tests/services/test_chat_policy.py tests/services/test_chat_response.py`
- Manual API check: `POST /api/ai/chat` with stale portfolio context returns refusal payload requesting context refresh.
</verification>

<success_criteria>
- `/api/ai/chat` behavior is policy-gated and context-grounded for clarify/refuse/answer modes.
- Contract fields required by Phase 7 success criteria are always present when applicable.
- Automated tests lock explainability, risk-adjusted recommendations, and stale/incomplete refusal behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-03-SUMMARY.md`.
</output>
