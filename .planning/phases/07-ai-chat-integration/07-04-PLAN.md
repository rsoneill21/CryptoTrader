---
phase: 07-ai-chat-integration
plan: 04
type: execute
wave: 1
depends_on: ["07-03"]
files_modified: ["backend/api/ai.py", "backend/tests/api/test_ai_chat_integration.py"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "AI orchestrator instructs AI to provide structured trade rationales"
    - "SSE stream contains trade_explanation metadata when a rationale is requested"
    - "Portfolio impact is delivered in the final SSE metadata frame"
  artifacts:
    - path: "backend/api/ai.py"
      provides: "Structured rationale orchestration and JSON parsing"
    - path: "backend/tests/api/test_ai_chat_integration.py"
      provides: "Verification of metadata delivery"
  key_links:
    - from: "backend/api/ai.py"
      to: "ChatResponseNormalizer"
      via: "contract normalization with trade_explanation"
---

<objective>
Enforce structured output from the AI for trade rationales and ensure all rich metadata (recommendations, impact, explanations) is delivered through the SSE stream.

Purpose: Fixes Gap 2 (Structural Enforcement) and ensures Gap 1/3 data is available for the UI.
Output: Backend orchestrator that instructs, parses, and emits structured trade rationale data.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ai-chat-integration/07-03-SUMMARY.md
@.planning/phases/07-ai-chat-integration/VERIFICATION.md
@backend/api/ai.py
@backend/services/chat_response.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prompt engineering for structured trade rationales</name>
  <files>backend/api/ai.py</files>
  <action>
    Modify `ChatAIService._compose_user_prompt` to include strict formatting instructions when the prompt involves trade rationales (detected by keywords like 'why', 'trade', 'rationale').
    
    Instructions must demand a JSON block at the end of the response containing:
    - thesis: A concise explanation
    - market_signals: A list of specific signals
    - risk_checks: A list of risk evaluations
    - counterfactual: What would invalidate this trade
    
    Example instruction: "If providing a trade rationale, append a JSON block formatted as: <rationale>{"thesis": "...", "market_signals": [], "risk_checks": [], "counterfactual": "..."}</rationale>"
  </action>
  <verify>Log the final prompt during a 'why trade' query and verify the instruction is present.</verify>
  <done>AI is instructed to produce structured JSON for trade explanations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured JSON parsing in AI stream</name>
  <files>backend/api/ai.py</files>
  <action>
    Update `_streaming_chat_response` in `backend/api/ai.py` to:
    1. Accumulate the full AI response text.
    2. Extract the `<rationale>` JSON block from the accumulated text.
    3. Parse the JSON and pass it as `trade_explanation` to `normalizer.normalize()`.
    4. Ensure `portfolio_impact` from the policy is also correctly captured and emitted in the final metadata frame.
    5. Strip the raw JSON block from the `summary_paragraph` so it doesn't appear as raw text in the UI.
  </action>
  <verify>Run a test using a mocked AI response containing a valid <rationale> tag and verify the final SSE frame contains the structured `trade_explanation` object.</verify>
  <done>Orchestrator correctly extracts and normalizes structured AI output.</done>
</task>

<task type="auto">
  <name>Task 3: Meta-data regression suite update</name>
  <files>backend/tests/api/test_ai_chat_integration.py</files>
  <action>
    Update the integration tests to verify:
    - 'answer' mode returns `recommendations` and `portfolio_impact` in the initial metadata frame.
    - 'why trade' queries return a final frame with a populated `trade_explanation` object.
    - `portfolio_impact` is present in the final metadata.
  </action>
  <verify>Run `backend/venv/bin/python -m pytest backend/tests/api/test_ai_chat_integration.py`</verify>
  <done>Backend metadata delivery is verified by regression tests.</done>
</task>

</tasks>

<success_criteria>
- Backend orchestrator successfully triggers structured rationale generation.
- SSE final frame contains `trade_explanation` and `portfolio_impact`.
- Integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-04-SUMMARY.md`
</output>
