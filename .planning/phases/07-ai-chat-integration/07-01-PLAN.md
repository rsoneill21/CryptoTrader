---
phase: 07-ai-chat-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/services/chat_context.py, backend/services/chat_policy.py, backend/services/chat_response.py, backend/tests/services/test_chat_context.py, backend/tests/services/test_chat_policy.py, backend/tests/services/test_chat_response.py]
autonomous: true
must_haves:
  truths:
    - "Prompt intent is classified into tactical/session or performance/24h-7d windows before generating a response."
    - "Broad prompts trigger a clarifying follow-up mode instead of immediate full recommendations."
    - "Stale or incomplete trading context produces explicit refusal metadata with refresh requirements."
    - "Elevated-risk recommendation planning defaults primary action to hold/no-action with exactly one safer backup option."
  artifacts:
    - path: "backend/services/chat_context.py"
      provides: "Context assembler with adaptive timeframe, top-position summary, baseline comparison, and freshness/incomplete checks"
    - path: "backend/services/chat_policy.py"
      provides: "Policy engine for clarify/refuse/answer modes and elevated-risk/aggressive-request guardrails"
    - path: "backend/services/chat_response.py"
      provides: "Normalized response contract and hybrid renderer (short paragraph + bullets)"
  key_links:
    - from: "backend/services/chat_policy.py"
      to: "backend/services/chat_context.py"
      via: "policy evaluation on assembled context flags"
      pattern: "evaluate\(.*context"
    - from: "backend/services/chat_response.py"
      to: "backend/services/chat_policy.py"
      via: "mode + guardrail fields mapped into response contract"
      pattern: "mode|guardrail|refusal_reason"
---

<objective>
Build the backend chat orchestration foundation so AI responses are grounded in live trading context and locked behavior rules before API streaming integration.

Purpose: Centralize context assembly, guardrail policy decisions, and deterministic response normalization so chat behavior is consistent and testable.
Output: New `chat_context`, `chat_policy`, and `chat_response` services with service-level regression tests.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-chat-integration/07-CONTEXT.md
@.planning/phases/07-ai-chat-integration/07-RESEARCH.md
@backend/api/ai.py
@backend/api/trades.py
@backend/api/risk.py
@backend/api/market.py
@backend/api/strategies.py
@backend/api/alerts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement trading-grounded chat context assembly service</name>
  <files>backend/services/chat_context.py, backend/tests/services/test_chat_context.py</files>
  <action>Create `ChatContextAssembler` that uses existing backend domain surfaces (trades/risk/market/strategies/alerts APIs or equivalent services) to build one normalized context payload with: `timeframe_used` (session/24h/7d), summary portfolio view + top positions, prior day/session baseline comparison, and freshness metadata. Enforce hard flags for stale/incomplete state (`stale_context`, `incomplete_context`, missing fields) including refusal reasons for expired portfolio snapshots, missing risk reference timestamps, or absent trade rationale context for "why-trade" prompts. Keep data access async and aligned with established AsyncSession patterns.</action>
  <verify>`cd backend && pytest tests/services/test_chat_context.py`</verify>
  <done>Service returns deterministic context objects with adaptive timeframe selection, baseline comparison, and explicit stale/incomplete detection that can drive refusal behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement chat policy engine for locked guardrails</name>
  <files>backend/services/chat_policy.py, backend/tests/services/test_chat_policy.py</files>
  <action>Create `ChatPolicyEngine` that evaluates prompt + assembled context and outputs `mode` (`clarify`, `answer`, `refuse`) with guardrail fields. Encode locked decisions exactly: medium default depth; broad prompts require clarifying follow-up first; recommendation output must be top recommendation + one backup; elevated-risk posture defaults primary action to hold/no-action; aggressive requests conflicting with risk posture return a safer alternative + rationale; portfolio impact appears only for position-changing recommendations; confidence is emitted only when user explicitly asks; losing-outcome tone markers are clinical/factual.</action>
  <verify>`cd backend && pytest tests/services/test_chat_policy.py`</verify>
  <done>Policy engine deterministically returns clarify/refuse/elevated-risk outcomes and recommendation constraints that satisfy the locked context decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add response contract normalization and hybrid renderer</name>
  <files>backend/services/chat_response.py, backend/tests/services/test_chat_response.py</files>
  <action>Implement response-contract normalizer/formatter that converts policy + model output into a stable structure (`mode`, `timeframe_used`, `summary_paragraph`, `bullets`, `recommendations`, `trade_explanation`, `guardrail`, `meta`). Enforce required "why-trade" elements (thesis, market signals, risk checks, counterfactual) and omit confidence unless requested. Add helper that renders the default hybrid answer style (short paragraph + bullets) while preserving refusal/clarify payloads.</action>
  <verify>`cd backend && pytest tests/services/test_chat_response.py`</verify>
  <done>Normalizer rejects malformed outputs, guarantees contract shape, and renders hybrid responses that match locked answer style and explanation requirements.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/services/test_chat_context.py tests/services/test_chat_policy.py tests/services/test_chat_response.py`
- Confirm tests cover: broad prompt clarify gate, stale/incomplete refusal flags, elevated-risk hold default, confidence-on-request only, and why-trade required fields.
</verification>

<success_criteria>
- Context assembler always returns adaptive timeframe + baseline comparison + freshness/incomplete flags.
- Policy engine outputs deterministic clarify/refuse/answer modes with locked recommendation behavior.
- Response normalizer provides one contract schema that downstream API streaming can trust.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-01-SUMMARY.md`.
</output>
