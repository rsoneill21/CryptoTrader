---
phase: 07-ai-chat-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/ChatWindow.js, frontend/src/services/api.js, frontend/src/pages/AIChat.js]
autonomous: true
must_haves:
  truths:
    - "Chat UI correctly parses SSE frames (`data: {...}`) instead of appending raw stream bytes."
    - "Chat history renders from backend turn-pair entries (`user_message`, `ai_response`) in chronological order."
    - "Assistant responses render hybrid output (short paragraph + bullets) when contract fields are present, with safe fallback to plain text."
  artifacts:
    - path: "frontend/src/components/ChatWindow.js"
      provides: "SSE parser, history mapping fix, and contract-aware response rendering"
    - path: "frontend/src/services/api.js"
      provides: "Chat stream/bootstrap helper and normalized AI error extraction"
    - path: "frontend/src/pages/AIChat.js"
      provides: "Preserved tone/alert context wiring with no regression in chat request payload shaping"
  key_links:
    - from: "frontend/src/components/ChatWindow.js"
      to: "/api/ai/chat"
      via: "fetch stream reader + SSE frame parser"
      pattern: "data:\\s*\\{"
    - from: "frontend/src/components/ChatWindow.js"
      to: "/api/ai/chat/history"
      via: "history normalization from user_message/ai_response"
      pattern: "user_message|ai_response"
---

<objective>
Fix chat frontend transport and rendering so streamed AI responses and persisted history display correctly against the backend chat contract.

Purpose: Remove protocol mismatch and history mapping bugs that currently prevent reliable conversational UX.
Output: Contract-aware `ChatWindow` behavior with robust SSE parsing, correct history hydration, and stable fallback rendering.
</objective>

<execution_context>
@/home/packnation82/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-chat-integration/07-CONTEXT.md
@.planning/phases/07-ai-chat-integration/07-RESEARCH.md
@frontend/src/components/ChatWindow.js
@frontend/src/pages/AIChat.js
@frontend/src/services/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement robust SSE frame parsing in ChatWindow</name>
  <files>frontend/src/components/ChatWindow.js</files>
  <action>Replace raw text append logic with SSE parsing by frame boundary (`\n\n`) and `data:` line extraction. Decode JSON payload per event and handle contract event types (`chunk`, `meta`, `guardrail`, `done`) while preserving compatibility with legacy `chunk`-only payloads. Keep streaming UX responsive, finalize assistant message on `done`, and surface refusal/error details without breaking message order.</action>
  <verify>`cd frontend && npm run lint`</verify>
  <done>Streaming output no longer shows raw `data:` fragments and assistant messages finalize cleanly from structured SSE events.</done>
</task>

<task type="auto">
  <name>Task 2: Correct chat history normalization and turn mapping</name>
  <files>frontend/src/components/ChatWindow.js</files>
  <action>Map `/api/ai/chat/history` entries from backend pair shape (`user_message`, `ai_response`, `timestamp`, `id`) into two chronological UI messages per record. Preserve stable IDs, maintain newest-last ordering, and avoid role/content assumptions that discard one side of a chat turn.</action>
  <verify>`cd frontend && npm run lint`</verify>
  <done>History view shows both user prompts and assistant replies for each persisted entry in correct temporal order.</done>
</task>

<task type="auto">
  <name>Task 3: Render contract-aware hybrid responses with safe fallbacks</name>
  <files>frontend/src/components/ChatWindow.js, frontend/src/services/api.js, frontend/src/pages/AIChat.js</files>
  <action>Add a thin client normalizer/helper for AI chat payloads so `summary_paragraph` + `bullets` can render as the default hybrid format, and optional guardrail/recommendation metadata can be displayed when present. Keep tone and alert-context request wiring unchanged in `AIChat.js`; do not move policy decisions client-side. If contract fields are absent, fall back to plain text rendering.</action>
  <verify>`cd frontend && npm run lint`</verify>
  <done>Chat UI consistently renders hybrid answers when provided, but remains backward-compatible with plain string assistant responses.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint`
- `cd backend && pytest tests/api/test_ai_chat_integration.py -k "stream or history"` (after backend plan completion)
- Manual smoke: load `/ai-chat`, confirm history includes user+assistant turns, send prompt, and verify streamed output never contains raw SSE protocol text.
</verification>

<success_criteria>
- SSE parser handles structured chat frames without UI corruption.
- History hydration reflects backend schema accurately.
- Hybrid answer rendering works with graceful fallback and no regressions to tone/alert context request payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-chat-integration/07-02-SUMMARY.md`.
</output>
