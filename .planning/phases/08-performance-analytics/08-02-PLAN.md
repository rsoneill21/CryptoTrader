---
phase: 08-performance-analytics
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [backend/api/performance.py, backend/api/main.py, backend/services/performance_service.py]
autonomous: true

must_haves:
  truths:
    - "API provides current Sharpe Ratio, Max Drawdown, and Win Rate"
    - "API provides historical equity and metrics data for charting"
    - "New snapshots are broadcasted to the frontend in real-time via SSE"
  artifacts:
    - path: "backend/api/performance.py"
      provides: "Analytics endpoints"
---

<objective>
Implement the analytics API endpoints to serve performance metrics and historical data to the frontend, including real-time updates via SSE.

Purpose: Expose calculated performance data for visualization.
Output: Performance API and real-time SSE broadcaster.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-analytics/08-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Performance API Implementation</name>
  <files>backend/api/performance.py, backend/api/main.py</files>
  <action>
    1. Create `backend/api/performance.py`:
        - `GET /api/performance/summary`: Returns the latest calculated metrics from the most recent snapshot.
        - `GET /api/performance/history`: Returns time-series data (equity + metrics) filtered by timeframe. Use daily anchors for history > 30 days. MUST support `strategy_id` and `asset_pair` filters.
        - `GET /api/performance/trades`: Returns a list of recent closed trades for the trade history view (include P&L, asset, type, timestamps).
    2. Register the router in `backend/api/main.py`.
  </action>
  <verify>
    Check endpoints with `curl`:
    - `curl http://localhost:8000/api/performance/summary`
    - `curl "http://localhost:8000/api/performance/history?timeframe=1w&strategy_id=1&asset_pair=BTC/USD"`
  </verify>
  <done>
    Performance API is functional and serving data from the snapshot engine.
  </done>
</task>

<task type="auto">
  <name>Task 2: SSE Broadcast Integration</name>
  <files>backend/services/performance_service.py, backend/api/performance.py</files>
  <action>
    1. In `backend/api/performance.py`, implement an SSE endpoint `GET /api/performance/stream` (reuse Phase 7 SSE logic).
    2. In `PerformanceService.capture_snapshot()`, after saving to DB, publish the new snapshot data to the SSE channel (e.g., via Redis or a shared event emitter).
  </action>
  <verify>
    Connect to `curl -N http://localhost:8000/api/performance/stream`. Trigger a `capture_snapshot()` and verify the data appears in the stream.
  </verify>
  <done>
    New performance data is broadcasted live to connected clients.
  </done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/08-performance-analytics/08-02-SUMMARY.md`
</output>
