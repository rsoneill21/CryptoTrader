---
phase: 08-performance-analytics
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [frontend/src/pages/PerformanceAnalytics.jsx, frontend/src/components/analytics/EquityCurveChart.jsx, frontend/src/components/analytics/PerformanceSummary.jsx, frontend/src/components/analytics/TradeHistoryTable.jsx, frontend/src/App.js, frontend/src/components/Sidebar.js, frontend/package.json]
autonomous: true

must_haves:
  truths:
    - "User can view a dashboard with Sharpe Ratio, Alpha, and Max Drawdown"
    - "Alpha percentage badge is prominently displayed in the dashboard header"
    - "User can see an Equity Curve chart showing Cash vs Asset value"
    - "User can view a complete list of past trades with P&L details"
    - "Dashboard updates live when new performance data is broadcasted"
  artifacts:
    - path: "frontend/src/pages/PerformanceAnalytics.jsx"
      provides: "Main analytics view"
    - path: "frontend/src/components/analytics/TradeHistoryTable.jsx"
      provides: "Detailed trade logs"
---

<objective>
Create the frontend dashboard to visualize trading performance metrics, the historical equity curve, and detailed trade history.

Purpose: Provide the user with actionable insights and transparency into strategy performance.
Output: Integrated Performance Analytics dashboard with real-time updates.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-analytics/08-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Performance Summary & Alpha Badge</name>
  <files>frontend/src/components/analytics/PerformanceSummary.jsx, frontend/src/pages/PerformanceAnalytics.jsx</files>
  <action>
    1. Create/Update `PerformanceSummary.jsx`:
        - Display key metrics: Sharpe Ratio, Win Rate, Max Drawdown, Volatility.
        - Implement real-time SSE listener for metric updates.
    2. In `PerformanceAnalytics.jsx` Header:
        - Add a prominent "Alpha" badge displaying the percentage active return vs benchmark.
        - Use color coding: Green for positive alpha, Red for negative.
  </action>
  <verify>
    Verify metrics and Alpha badge render correctly. Simulate an SSE event to check live updates.
  </verify>
  <done>
    Summary metrics and Alpha badge are visible and update in real-time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Equity Curve Chart</name>
  <files>frontend/src/components/analytics/EquityCurveChart.jsx, frontend/package.json</files>
  <action>
    1. Install dependency: `npm install lightweight-charts` in `frontend/`.
    2. Implement `EquityCurveChart.jsx` using `lightweight-charts`:
        - Stacked Area Chart:
            - Series 1: Cash balance (Base layer).
            - Series 2: Asset value (Stacked on top).
        - Line Overlay: Benchmark Buy & Hold performance.
        - Features: Timeframe selection (1D, 1W, 1M, ALL), tooltips showing total equity at point.
        - Live updates: Append new points from SSE stream.
  </action>
  <verify>
    Chart renders with multiple series. Check that switching timeframes fetches new data from `/api/performance/history`.
  </verify>
  <done>
    Interactive equity curve chart is functional with live updates.
  </done>
</task>

<task type="auto">
  <name>Task 3: Trade History Table</name>
  <files>frontend/src/components/analytics/TradeHistoryTable.jsx</files>
  <action>
    Create `TradeHistoryTable.jsx`:
    - Columns: Asset Pair, Type (Buy/Sell), Entry Price, Exit Price, P&L (%), P&L (Amount), Timestamp.
    - Logic: Fetch from `GET /api/performance/trades`.
    - Styling: Use Tailwind for a clean, paginated table suitable for the dark theme.
    - Integration: When a new trade closes (via SSE), prepend it to the table.
  </action>
  <verify>
    Table displays trade data with correct P&L formatting. Verify prepending on new trade closure.
  </verify>
  <done>
    Trade history is accessible and searchable within the analytics dashboard.
  </done>
</task>

<task type="auto">
  <name>Task 4: Routing & Integration</name>
  <files>frontend/src/pages/PerformanceAnalytics.jsx, frontend/src/App.js, frontend/src/components/Sidebar.js</files>
  <action>
    1. Assemble `PerformanceAnalytics.jsx` with Summary, Chart, and Trade History components.
    2. Register route `/performance` in `App.js`.
    3. Add "Performance" link to `Sidebar.js`.
  </action>
  <verify>
    Navigate to /performance. Ensure all components load and handle the dark mode theme consistently.
  </verify>
  <done>
    Performance dashboard is fully integrated and accessible from the sidebar.
  </done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/08-performance-analytics/08-03-SUMMARY.md`
</output>
