---
phase: 08-performance-analytics
plan: 04
type: execute
wave: 1
depends_on: [08-01]
files_modified: [.planning/ROADMAP.md, .planning/STATE.md, backend/tests/api/test_performance.py, backend/tests/services/test_performance_service.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Performance API integration tests are present and passing"
    - "Performance service logic for snapshot capture and retention is verified via tests"
    - "Project ROADMAP.md and STATE.md accurately reflect Phase 08 completion"
    - "Summary for 08-02 is formalized and 08-03 is properly tracked"
  artifacts:
    - path: "backend/tests/api/test_performance.py"
      provides: "API regression coverage"
    - path: "backend/tests/services/test_performance_service.py"
      provides: "Service logic verification"
---

<objective>
Close the critical testing and tracking gaps for Phase 08. This plan formalizes the existing Performance API and Dashboard implementations, adds missing integration and service tests, and synchronizes the overall project state to reflect Phase 08 completion.

Purpose: Ensure reliability of performance analytics and maintain accurate project tracking.
Output: Verified performance subsystem and synchronized planning docs.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-analytics/08-01-SUMMARY.md
@backend/api/performance.py
@backend/services/performance_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Performance Subsystem Tests</name>
  <files>backend/tests/api/test_performance.py, backend/tests/services/test_performance_service.py</files>
  <action>
    1. Create `backend/tests/api/test_performance.py`:
        - Test `GET /api/performance/summary`: Ensure it returns valid fields or defaults when no snapshots exist.
        - Test `GET /api/performance/history`: Verify timeframe filters (`1w`, `1m`, etc.) return correct subsets of data.
        - Test `GET /api/performance/trades`: Verify it returns closed trades with P&L details.
        - Test `GET /api/performance/stream`: Smoke test SSE endpoint connection and headers.
    2. Create `backend/tests/services/test_performance_service.py`:
        - Mock `kraken_service` and `paper_trading_engine` to test `capture_snapshot()`.
        - Verify `quantstats` metrics (Sharpe, etc.) are calculated and sanitized correctly.
        - Verify `cleanup_old_snapshots()` correctly implements the tiered retention policy (pruning to daily anchors).
  </action>
  <verify>
    Run `pytest backend/tests/api/test_performance.py backend/tests/services/test_performance_service.py` and ensure all pass.
  </verify>
  <done>
    Performance API and Service have full regression coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Formalize 08-02/08-03 & Sync State</name>
  <files>.planning/phases/08-performance-analytics/08-02-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    1. Create `.planning/phases/08-performance-analytics/08-02-SUMMARY.md`:
        - Document the implementation of `backend/api/performance.py`.
        - Note SSE integration via `Channels.PERFORMANCE`.
    2. Update `.planning/ROADMAP.md`:
        - Mark 08-01, 08-02, and 08-03 as completed (`[x]`).
        - Ensure Phase 8 goal is marked as met.
    3. Update `.planning/STATE.md`:
        - Move current position to Phase 9.
        - Update completion stats (Phase 8 is now 100% complete).
        - Add Phase 8 summary of results (Engine, API, Dashboard).
  </action>
  <verify>
    Check ROADMAP.md and STATE.md for accuracy. Ensure summaries exist in the phase directory.
  </verify>
  <done>
    Project state is synchronized and Phase 08 is formally closed.
  </done>
</task>

</tasks>

<success_criteria>
1. All performance-related tests pass in CI environment.
2. Dashboard and API are verified as functional and tracked.
3. ROADMAP.md and STATE.md show Phase 08 as complete.
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-analytics/08-04-SUMMARY.md`
</output>
