---
phase: 08-performance-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/requirements.txt, backend/db/models.py, backend/services/performance_service.py, backend/core/celery_app.py, backend/agents/trade_executor.py]
autonomous: true

must_haves:
  truths:
    - "System captures portfolio equity (Cash + Assets) periodically"
    - "System captures portfolio equity immediately after a trade closes"
    - "PerformanceSnapshot stores calculated metrics (Sharpe, Alpha, etc.) for historical fidelity"
    - "Tiered retention policy exists: hourly for 30 days, daily thereafter"
  artifacts:
    - path: "backend/db/models.py"
      provides: "PerformanceSnapshot model with metrics fields"
    - path: "backend/services/performance_service.py"
      provides: "Snapshot capture logic using MarketAnalyst"
  key_links:
    - from: "backend/services/performance_service.py"
      to: "MarketAnalyst.get_indicator_summary"
      via: "Price source for snapshots"
---

<objective>
Implement the core snapshot engine to record portfolio performance data and metrics over time. This foundation stores both raw equity data and derived financial metrics for historical analysis.

Purpose: Track portfolio value and performance quality (Sharpe, Alpha) over time.
Output: PerformanceSnapshot table with metrics and automated capture/retention mechanisms.
</objective>

<execution_context>
@/home/packnation82/.gemini/get-shit-done/workflows/execute-plan.md
@/home/packnation82/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/db/models.py
@backend/agents/market_analyst.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Performance DB Model & Migration</name>
  <files>backend/db/models.py</files>
  <action>
    Add `PerformanceSnapshot` model to `backend/db/models.py`:
    - id: UUID
    - timestamp: DateTime (UTC)
    - total_equity: Decimal
    - cash_balance: Decimal
    - asset_value: Decimal
    - strategy_id: String (Optional)
    - asset_pair: String (Optional)
    - metadata: JSONB (Store asset prices used)
    - Metrics (Historical Fidelity):
        - sharpe_ratio: Float
        - sortino_ratio: Float
        - max_drawdown: Float
        - win_rate: Float
        - alpha: Float
    
    Then, create and run an Alembic migration: `alembic revision --autogenerate -m "add performance snapshots"` and `alembic upgrade head`.
  </action>
  <verify>
    Check if `performance_snapshots` table exists with new metric columns: `psql $DATABASE_URL -c "\d performance_snapshots"`.
  </verify>
  <done>
    `performance_snapshots` table is created in the database with metrics support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Snapshot Capture Logic with Metrics</name>
  <files>backend/requirements.txt, backend/services/performance_service.py</files>
  <action>
    1. Add `quantstats` to `backend/requirements.txt`.
    2. Implement `PerformanceService` in `backend/services/performance_service.py`:
        - `capture_snapshot(strategy_id=None)`:
            - Fetch balances from `portfolio_service`.
            - Fetch current prices from `MarketAnalyst.get_indicator_summary(symbol)` (using memory cache to avoid rate limits).
            - Calculate `total_equity`.
            - Fetch recent equity history from DB.
            - Use `quantstats.stats` to calculate current Sharpe, Sortino, Max Drawdown, and Win Rate.
            - Calculate Alpha (Active Return - Benchmark Return) relative to a benchmark (e.g., BTC/USD price returns starting from session anchor).
            - Persist all fields to `PerformanceSnapshot`.
  </action>
  <verify>
    Call `capture_snapshot()` from a script. Verify the DB record has non-null values for equity AND metrics (Sharpe, Alpha, etc.).
  </verify>
  <done>
    `PerformanceService` correctly calculates and stores portfolio equity and real-time metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Automated Triggers & Tiered Retention</name>
  <files>backend/core/celery_app.py, backend/agents/trade_executor.py, backend/services/performance_service.py</files>
  <action>
    1. In `backend/core/celery_app.py`: Register an hourly task `capture_periodic_snapshot`.
    2. In `backend/agents/trade_executor.py`: Trigger `capture_snapshot` when an order reaches `CLOSED` status.
    3. Implement `cleanup_old_snapshots()` in `PerformanceService`:
        - Delete snapshots older than 30 days EXCEPT for the first snapshot of each day (the "Closing" value).
    4. Register `cleanup_old_snapshots` as a daily Celery task in `backend/core/celery_app.py`.
  </action>
  <verify>
    1. Verify Celery beat schedule for hourly capture and daily cleanup.
    2. Manually trigger `cleanup_old_snapshots` with mock old data to ensure hourly points are pruned while daily anchors remain.
  </verify>
  <done>
    Snapshots are captured automatically and managed by a tiered retention policy.
  </done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/08-performance-analytics/08-01-SUMMARY.md`
</output>